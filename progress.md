# Project Progress — FreelancePaymentProtection

> Persistent memory across sessions. READ THIS FILE BEFORE doing anything else in future sessions. Do not delete historical progress — append/update carefully.

## Product

Name: FreelancePaymentProtection
Current Version: 0.19.0 (session 24: launch product — public home/pricing/FAQ/privacy/terms/contact/onboarding + feedback, 357 tests green; post-24 fixes: CI DATABASE_URL env, @prisma/client moved to dependencies for Docker runtime, Framer DESIGN.md dark-canvas redesign + scroll reveal, migration naming unification below)
Current Stage: Session 25 — LIVE at freelance.lab.yashdev.tech (/health db:"up"); remaining: homepage + first-workflow self-verify, Stripe later, uptime monitor

## Core Product Thesis

Freelancers know they should use deposits, milestones, approvals, payment deadlines and final-delivery locks, but in practice the workflow is fragmented across email, spreadsheets, invoices, cloud storage and manual reminders. This product is a **payment-protection workflow** (not an invoice generator, CRM, contract-template site, or reminder app) that ties payment to project progress, retains freelancer leverage until contractual conditions are met, treats clients professionally, and maintains a provable financial state for every project. North-star direction: Protected Project Value + honestly-measured payment leakage collected — not invoice counts.

## Current Architecture

Frontend: Calm server-rendered pages — freelancer workspace (`/app/*`, Session 04) + client portal (`/portal/:pid?token=…`, Session 07, `/portal/app.js` actions, no inline script/style so CSP holds)
Backend: Node 24 + TypeScript (strict) + Fastify 5 — `src/server.ts` boot, `src/config/app.ts` (helmet CSP/HSTS/nosniff/frame-deny, global rate-limit, typed error envelope, injectable Store seam), routes: `/health`, `/`, `/api/v1/disclaimer`, `/api/v1/auth/*` (signup/signin/signout/me), `/api/v1/workspaces*` (CRUD + members), `/api/v1/workspaces/:id/{clients,projects}*` + `/api/v1/workspaces/:wid/projects/:pid/milestones*` (CRUD + amount + 17 guarded transitions + reorder, Session 05) + `/api/v1/workspaces/:wid/projects/:pid/agreements*` (draft versions + send/accept/void, hash-pinned, Session 06) + `/api/v1/workspaces/:wid/projects/:pid/portal-links*` (issue/list/revoke, Session 07) + `/api/v1/portal/:pid/*` (overview/approve/request-revision/reject/dispute/pay/agreement-accept via magic link, Session 11 adds reject/dispute) + `/api/v1/workspaces/:wid/projects/:pid/milestones/:mid/deliverables*` + `/api/v1/workspaces/:wid/projects/:pid/deliverables/:did/*` (create/versions/submit/share-preview/mark-review/mark-payment-pending/mark-paid/release/staging/transfer + preview/final signed downloads, Session 10) + `/api/v1/portal/:pid/deliverables*` (client-safe list/detail/preview/final/approve/revision/reject/dispute/approvals via magic link, Session 11 adds revision/reject/dispute/history) + freelancer approval history `GET .../deliverables/:did/approvals` + `GET .../milestones/:mid/approvals` (Session 11) + reminder engine (Session 12: workspace `.../reminder-policy` GET/PUT + project `.../reminders/policy` GET/PUT + milestone `.../reminders/plan|schedule` + milestone/project `GET .../reminders` audit views + `POST /api/v1/workspaces/:wid/reminders/:rid/send|retry|cancel` + project `POST .../reminders/run-due` tick + `GET /api/v1/reminder-templates`) + payment plans (Session 13: milestone `POST .../payment-plans` propose + milestone/plan `GET` detail + `POST .../:planId/accept|installments/:seq/mark-paid|run-due|default` + project `GET .../payment-plans` history + portal `GET|POST .../portal/:pid/payment-plans*` list/detail/accept via magic link)
Database: Postgres 16-first via Prisma 6 — `prisma/schema.prisma` + `prisma/migrations/0001_foundation/migration.sql` (all 13 domain tables, UUID PKs, integer cents, UTC timestamptz, idempotency uniques, append-only triggers on events/payments/approvals/agreements/evidence_packs) + `0002_client_project_domain` (Session 04) + `0003_milestone_engine` (Session 05: milestone currency/5-dimension states/idempotency/audit columns, nullable dueDate, UNIQUE(projectId, orderIndex), amount>0 CHECK; Event.seq now `@default(autoincrement())`) + `0004_agreement_terms` (Session 06: structured business-term columns, status lifecycle, version/status CHECKs, `guard_agreement_immutability()` trigger replacing the blanket no-update trigger on agreements — content/acceptance immutable, lifecycle transitions allowed) + `0009_reminder_engine` (Session 12: notification audit columns — templateVersion/subject/bodySnapshot/recipientName/deliveredAt/canceledAt/attemptCount/lastError/nextActionAt/nextActionLabel/trigger/policyStep/policyVersion/idempotencyKey UNIQUE — plus `projects.reminder_policy` JSONB default `{}`) + `0010_payment_plans` (Session 13: `currency`/`version`/`supersedesId` self-reference/`note`/`updatedAt`, `PlanState.superseded`, `original_amount_cents > 0` CHECK, project index). No migration applied to a live DB yet (sessions are stateless; schemas re-validated).
Authentication: `src/lib/auth.ts` (scrypt hash/verify) + `src/lib/session.ts` (stateless HMAC `s1.{userId}.{iat}.{exp}.{nonce}.{sig}`, Bearer + HttpOnly cookie, 7d TTL, constant-time verify) + `src/lib/authz.ts` (membership/role/IDOR gates, generic 403) + `src/routes/requestAuth.ts` (`requireAuth`: identity only from verified session). Per-route stricter rate limit on signup/signin. Client magic-link crypto: `src/lib/magicLink.ts` (HMAC-SHA256, project scope, expiry, rotation, sha256 store; `PortalLink.tokenHash` unique).
Payments: Stripe-behind-interface (`src/lib/providers.ts` PaymentProvider + Stripe/Fake/Noop). Checkout sessions + refunds + verified retrieve via the seam; cards enter on hosted checkout only, never the API. Webhook security: `src/lib/webhook.ts` (Stripe-style `t,v1` verify + timestamp tolerance) + `POST /api/v1/webhooks/payments` (signature-is-credential, persistent provider-event-id dedupe via `WebhookReceived` marker + in-memory fast path, exact amount/currency match, mismatch → needs-review, out-of-order reversals keep local state with a note, success page never marks paid). Lifecycle `created→pending→processing→paid` + `failed|cancelled` + `paid→refunded|disputed` in pure `src/domain/payments.ts`; every transition appends an auditable `Payment*` event. Trust layer (Session 09): pure `src/domain/reconciliation.ts` (verification tiers claimed/initiated/provider_confirmed/settled/failed/reversed, `reconcileProject()` over internal rows vs verified receipts vs milestone invoices vs project total, 18 mismatch codes, `buildPaymentHistory()` plain-language timeline); freelancer `.../payments/reconciliation` + `/history` + `/diagnostics` + `POST .../reconciliation/run` (project-wide provider read-back) + `POST .../milestones/:mid/claim` (claimed≠paid) + client `POST /api/v1/portal/:pid/claim`. Financial history append-only — mismatches resolve via new events, never edits. No live keys; no escrow (invariant preserved).
Storage: `StorageProvider` seam + Noop (dev default) / Fake (tests: in-memory puts + `https://storage.example/signed/<key>?exp=<ttl>` URLs) + `resolveStorageProvider()`; deliverable finals policy enforced in routes (preview ≤1h once reviewable, final ≤15min only when released, 423 LOCKED otherwise; no bucket wired yet)
Email: `EmailProvider` seam + Noop (dev default) / Fake (tests: in-memory sent log + `failNext()` failure seam for retry-path tests) + `resolveEmailProvider()`; reminder engine sends neutral system-voice mail via the seam (Session 12)
AI: Workflow-scoped assistive drafts only (`src/domain/aiAssist.ts` + `src/routes/aiAssist.ts`, Session 17: extractive local engine `assistive-v1`, 5 helpers, review-required, no financial writes, no chatbot)
Validation: Zod at all boundaries (`src/lib/validate.ts`, `src/config/env.ts`)
Logging: Pino JSON + secret redaction (`src/lib/logger.ts`); scoped child loggers for workspace/project
Error handling: `AppError` codes → stable `{error:{code,message,details?,requestId}}` envelope; generic 403 (no oracle leak); 500s hide internals in production
Domain: Pure DB-free rules in `src/domain/` — `types.ts` (branded ids, 3-dimension states, 42-event vocabulary incl. ApprovalRejected/ApprovalDisputed + ReminderCancelled + 5 PaymentPlan events), `money.ts` (integer-cents math), `events.ts` (event-sourced reducer incl. ApprovalRejected no-op + ApprovalDisputed→disputed + PaymentPlanAccepted→plan_active + PaymentPlanCompleted→paid + PaymentPlanDefaulted→overdue), `release.ts` (`canRelease()` + version-pinned approval), `trust.ts` (ladder defaults), `paymentPlans.ts` (Session 13: exact-sum schedule validation, 1–12 installments, `buildSchedule()` seq assignment, `summarizePlan()` paid/remaining/next-due/missed/complete, `installmentView()` derived overdue, guarded `offered→active→completed|defaulted→superseded` transitions, verified-only `markInstallmentPaid()`, explicit `markInstallmentMissed()`, `cancelOpenInstallments()` on supersede, stable `plan:*` idempotency keys, professional system-voiced `renderInstallmentReminder()`), `reminders.ts` (Session 12: configurable policies — project override → workspace defaults → DEFAULT T-3/T+0/T+3/T+7/T+14/T+21, validation rails incl. manual-only escalation tones, `planReminderSchedule()` due-date anchoring, 6 professional system-voice templates with jurisdiction-aware escalation framing + banned-phrasing guard, `{{var}}` rendering, stable idempotency keys), `approvals.ts` (Session 11: formal approval audit — decisions approved/revision_requested/rejected/disputed, versionNo XOR versionRef pinning, note rules, isApprovalCurrent/latestDecision/effectiveDecisionForVersion/deriveApprovalEffect, event/label mapping), `deliverables.ts` (Session 10: 8-state controlled-delivery lifecycle, `canClientReview()` vs `canClientReceiveFinal()` separation, `checkRelease()` approval + verified-paid gate with flagged manual override, staging `none→staging_live→transfer_pending→transferred`, `HONEST_LIMITS` no-DRM notice, preview/final TTL policy), `milestone.ts` (Session 05: 5-dimension milestone engine — work/payment/approval/deliverable/unlock enums, 17 guarded transitions, DEFAULT/FLEXIBLE/STRICT workflow configs, audited amount changes, sequence validation, unlock derivation; `docs/milestone-workflow.md` renders the table), `agreement.ts` (Session 06: 11-section payment-terms validation, canonical text rendering, sha256 fingerprints, draft→sent→accepted→superseded/voided transitions, hash verify; `docs/agreement-terms.md` renders the model), `portal.ts` (Session 07: client-safe projection — totals/paid/due-now/remaining, focus headline, per-milestone stage + lock reasons, verified-payments-only, allowlisted activity labels, current-agreement card, next steps; never accepts freelancer-only fields)
Tests: Vitest — 277 tests (unit + HTTP integration) covering every security-checklist invariant + session/authz/IDOR/roles + milestone engine + agreement layer + client portal + payment lifecycle/checkout/webhooks/reconcile + trust reconciliation/history/diagnostics/claims/out-of-order/partial/run + deliverable lifecycle/review-vs-final/staging/safe-handling/gated-release API + formal approval audit (version pinning, stale-reset, revision/reject/dispute, idempotent repeats, tenant/portal boundaries) + reminder engine (policy validation/resolution/scheduling/rendering/jurisdiction guard + configurable policy API + idempotent schedule + audit views + send-once + fail/retry + cancel + manual-only escalation + run-due tick + paid-stop + tenant isolation) + payment plans (exact-sum validation, state machine, installment paid/missed/cancel, reminder copy, propose/accept/portal-accept/run-due idempotent reminders/verified-only mark-paid/modified-version preservation/completion-pays-milestone/default guards/history/tenant isolation) + evidence timeline (categories/headlines/filtering/detail/portal-safe/immutable/pause-unpause) + evidence packs (factual snapshot/hash/pins/printable/banned-phrase guard) + protection checks (12 observable conditions, evidence-cited, no client labels, no scores, API + Project-health page + tenant isolation); all green
CI: `.github/workflows/ci.yml` (validate→generate→typecheck→lint→format→build→test on Node 24); local `docker-compose.yml` (Postgres 16); `README.md` run instructions

## Completed

### 2026-09-18 — Session 24 (Launch product — first real external users, v0.19.0)

- Read progress.md + inspected repo (v0.18.3 baseline: Fastify 5 + Prisma 6 + SSR calm pages, Stripe-behind-seam, 353 tests green). No architecture change — one new public-site module plus version bump. All prior invariants preserved.
- VERIFIED 13 LAUNCH FLOWS END-TO-END (existing `tests/integration/endToEnd.test.ts`, 11 tests, all green — no product change needed): (1) signup creates account+workspace, (2) client CRUD, (3) project CRUD + summary, (4) milestone structure + 17 guarded transitions, (5) portal-link issue → magic-link overview/accept, (6) checkout → signed webhook → verified paid, (7) deliverable versions → submit/share-preview/mark-review → portal preview, (8) portal approve/revision pinned to version, (9) mark-payment-pending/mark-paid/release → gated final downloads (423 pre-release), (10) reminder plan/schedule + run-due tick + paid-stop, (11) pause/unpause recorded as events, (12) append-only timeline (405 on writes), (13) evidence-pack generate/list/detail with sha256 + drift reporting. Race verdict from Session 21 still holds (duplicate webhook → single duplicate:true).
- NEW PUBLIC LAUNCH SITE (`src/routes/site.ts`, wired in `src/config/app.ts`): `GET /` content-negotiates (browsers → marketing homepage HTML; API clients → existing JSON envelope, backwards compatible); public `GET /pricing /faq /privacy /terms /contact /onboarding` (short public cache, indexable meta descriptions, shared calm stylesheet, CSP-safe no inline script/style); authenticated `GET /app/onboarding` (live workspace checklist: account → client → project → milestones/due-dates → portal link → verified payment, with ✓/○ + next-step links); public `POST /api/v1/feedback` (zod-validated {message 10–2000, optional name/email/page, category feedback/contact/bug/idea}, 201 receipt {id, receivedAt}, logged + process-memory inbox with `feedbackInbox/clearFeedbackInbox` exports; documented limitation: wire to mailbox/ticket queue before scale).
- HONEST CLAIMS: every public page states informational-record / not-legal-advice / jurisdiction-dependent / no promised outcomes / no escrow / no custody; FAQ "Does this promise I will get paid?" answers No. New `tests/integration/launch.test.ts` sweeps all 7 public pages for the 4 banned claims (guaranteed payment / legally enforceable everywhere / stops fraud / clients cannot scam you) — absent.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 357/357 tests: 353 pre-existing + 4 new), `npm run build` compiles. Bumped 0.18.3→0.19.0 (`package.json`, `APP_VERSION`, http + launch tests). No secrets committed. No git push (unattended).
- Files changed — new: `src/routes/site.ts`, `tests/integration/launch.test.ts`; edited: `src/config/app.ts` (site wiring, `/` moved to negotiation), `src/routes/health.ts` + `package.json` + `tests/integration/http.test.ts` (0.19.0), `progress.md` (this entry).
- Best next step: wire `POST /api/v1/feedback` to a real mailbox/ticket queue + add a real signup HTML page (currently API-only) so onboarding step 1 is one click; then storage bucket `Content-Disposition: attachment` + real email provider (open since Session 19/22).

### 2026-09-18 — Session 23 (Skeptical product review — final critique, no code change)

- Read progress.md + inspected repo (v0.18.3: Fastify 5 + Prisma 6 + SSR calm pages, Stripe-behind-seam with Noop fail-closed in prod, Noop email/storage defaults, 353 tests green). No architecture change, no code change — review-only session. All prior invariants preserved.
- Produced `docs/final-product-critique.md` (role: freelancer who has used HoneyBook/Bonsai/Dubsado, Stripe, Docs, Gmail, and survived non-payment; "why would I use this instead?"). Ranked severe (S1–S6) / important (I1–I10) / nice-to-have (N1–N10), grounded in actual modules: 5-dim milestones + 17 transitions, 11-section agreements, magic-link portal, Stripe-only verified tiers + 18 mismatch codes, 8-state deliverables + staging/transfer, 4-decision version-pinned approvals, 3-level reminder policies, exact-sum plans, 11-category timeline, sha256 evidence packs, 12 protection checks, assistive-v1 AI, 18-kind notification inbox.
- WHAT SHOULD CHANGE: (1) close the Stripe-only gap — freelancer-attested manual/offline payment confirmation with receipt hash (new tier `manual_confirmed_by_freelancer`, audited as new event, never an edit); (2) real invoice object — numbers, tax/VAT, PO field, PDF, CSV/Xero export; (3) client link UX — rotatable non-cliff links, branded, no-JS approve/pay basics, SMS/WhatsApp option; (4) Simple Mode default (Agree → Approve → Pay → Release; hide workflow modes/staging/diagnostics/matrices/AI pages under Advanced); (5) merge money views (reconciliation+history+diagnostics → one Money card) and proof views (timeline+packs+protection+AI-summarize → one Record card); kill notifications inbox (keep event log); (6) project templates + 10-minute wizard + Gmail/Docs import; (7) trust-ladder "trusted client" flag (longer pause window, lighter cadence, release-on-approval+grace) + retainers/Net terms; (8) wire real email + storage providers so automations actually run.
- WHAT SHOULD NOT CHANGE: append-only events/payments/approvals/agreements/evidence (triggers + 405s); claim-never-paid separation + verification tiers honesty; version-pinned approvals + stale-reset; preview-vs-final gate + 423 LOCKED + flagged manual override; neutral system voice + banned-phrasing guards + manual-only escalation + jurisdiction framing + disclaimers; tenant isolation + generic 403/401s; idempotency everywhere; integer-cents money math; fail-closed prod behavior (Session-22 Noop 502s). These are the moat — simplify around them, never weaken them.
- RECOMMENDED NEXT IMPLEMENTATION SESSION (Session 24 — "first dollar in 10 minutes"): manual/offline payment confirmation with receipt evidence + invoice PDF export + project-template wizard + non-expiring rotatable client link. Concretely: new `manual_confirmed_by_freelancer` tier + `POST .../payments/:id/confirm-manual {method, reference, receiptHash}` (write-role, audited event, counts toward release like verified but labelled distinctly); invoice PDF render (number + tax + terms + pay link); 3 starter templates; portal grace page for expired links. Do NOT build: more evidence views, more AI helpers, more notification prefs, SMS gateway, multi-provider abstraction.
- VERIFIED: docs-only change — no code, no migration, no version bump (stays 0.18.3). Confirmed `docs/final-product-critique.md` exists and is the only new file; no secrets touched; no git push (unattended).
- Files changed — new: `docs/final-product-critique.md`; edited: `progress.md` (this entry).

### 2026-09-18 — Session 22 (Engineering lead — production-readiness audit, v0.18.3)

- Read progress.md + inspected repo (Session-21 end-to-end proof intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 347 tests green). No architecture change — every edit is a production hardening fix or a new deployability artifact. All prior invariants preserved.
- AUDIT (all areas from the task): env config fail-closed + required/optional matrix verified (`src/config/env.ts`); secrets — only placeholders in `.env.example`/compose/CI, none committed, `.gitignore` covers `.env`; logging Pino JSON + redaction paths verified, no debug logging in src (only fatal boot `console.error`); monitoring — `/health` liveness/readiness split + `dbLatencyMs` present, no vendor (documented baseline); error reporting — typed envelope, 500s hide internals in prod, single choke point `registerErrorHandler` for a future Sentry hook; webhooks — Stripe-style verify + tolerance + persistent dedupe, fail-closed 502 without secret; email/storage — Noop-in-prod seams confirmed safe-but-inert; rate limits global 120/min + 30/min auth; security headers helmet CSP-self/HSTS-prod/nosniff/frame-deny verified; caching — NONE on static assets (fixed); DB indexes — all hot scopes covered, no full-scan paths; pagination — timeline/notifications bounded + event caps, workspace lists unbounded-but-scoped (documented, fine at solo scale); query efficiency — `Promise.all` fan-out, no N+1, integer-cents math; frontend perf — SSR + tiny deferred scripts, no framework; a11y — lang/viewport/labels/focus-visible/roles present, skip-link + nav-label missing (fixed); SEO — no public marketing surface (all pages auth/token-gated), meta/robots missing (fixed); deployment — no Dockerfile, dev-only `migrate dev` script (fixed); builds — `tsc` clean, prisma valid.
- REMOVED / CLOSED: insecure dev shortcut — checkout/refund against the Noop provider in production could create `noop://` payment/refund rows that look real (now 502 `PROVIDER_ERROR` fail-closed when `isProduction && provider.name === "noop"`; portal pay stays intent-only best-effort). No demo secrets, fake data, debug logging, or TODOs hiding prod problems found in src (example/fake URLs exist only in test fixtures + test-only Fake providers, correctly gated by `NODE_ENV`/injection). No changes needed to migrations (0001→0011 sequence complete, schema validates).
- CODE CHANGES: `src/routes/payments.ts` (noop fail-closed on checkout + refund); `src/routes/pages.ts` (`Cache-Control: public, max-age=300` on styles/app.js, meta description on landing); `src/routes/portal.ts` (same caching on portal JS, meta description + robots noindex on portal + error pages); `src/ui/components.ts` (meta description + robots noindex on layout/unauthenticated, skip link + `#main-content` + nav aria-label, `.skip` CSS); `package.json` (new `prisma:deploy` = `migrate deploy` for production); new `Dockerfile` (multi-stage Node 24, migrations via `prisma:deploy`) + `.dockerignore`. Bumped 0.18.2→0.18.3.
- DOCS (new, solo-founder-ready): `docs/deployment.md` (env matrix, DB migrate, Docker/Node flows, Stripe/mail/bucket wiring, TLS, verify checklist, known non-goals) + `docs/operations.md` (health/logs, cron ticks table with curl, pg_dump backups, rotation, abuse signals, perf notes, weekly checklist) + `docs/incident-response.md` (SEV levels, 5-min triage, 7 runbooks, never-edit rule, post-incident template).
- TESTS: new `tests/integration/productionReadiness.test.ts` (6 tests: prod-noop checkout 502 + empty ledger, seeded-paid refund 502 + row untouched, 3 static assets cached, freelancer page skip/description/noindex/nav-label, portal page description/noindex/main). Full `npm run checks` green (typecheck + eslint zero-warnings + prettier + 353/353 tests: 347 pre-existing + 6 new), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set; bare-shell failure is the documented fail-closed env requirement, not a schema issue). No secrets committed. No git push (unattended).
- Files changed — new: `docs/deployment.md`, `docs/operations.md`, `docs/incident-response.md`, `Dockerfile`, `.dockerignore`, `tests/integration/productionReadiness.test.ts`; edited: `src/routes/payments.ts`, `src/routes/pages.ts`, `src/routes/portal.ts`, `src/ui/components.ts`, `package.json`, `src/routes/health.ts`, `tests/integration/http.test.ts`, `progress.md` (this entry).
- Best next step: wire the production storage bucket with `Content-Disposition: attachment` + `nosniff` (still open since Session 19), then connect a real email provider so the outbox actually delivers.

### 2026-09-18 — Session 21 (End-to-end verification — coherent-system proof)

- Read progress.md + inspected repo (Session-20 design pass intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 336 tests green). No architecture change — one new integration test file exercising the real HTTP API top to bottom. All prior invariants preserved.
- NEW `tests/integration/endToEnd.test.ts` (11 tests, sequential, shared fixtures where the narrative requires it, isolated projects where state would interfere):
  - FLOW 1: signup → client → project → 2 dated milestones; summary milestoneCount 2; ProjectCreated in trail.
  - FLOW 2: agreement draft → send → portal link → overview (pending_acceptance) → portal accept → checkout → signed webhook → milestone paid → portal paidCents 50000.
  - FLOW 3: request_funding → (reminder schedule while pending) → confirm_funding → start_work → submit → deliverable v1 → submit/share-preview/mark-review → portal preview → portal revision → v2 → share-preview/mark-review → portal approve v2 → approvals history shows approved@v2, effective.isApproved true.
  - FLOW 4: reschedule refused with 422 (funded = paid-stop, intended) → send scheduled reminder (email seam) → checkout → webhook paid → mark-payment-pending/mark-paid/release → freelancer + portal final downloads 200.
  - FLOW 5: past-due milestone → mark_overdue → reminder schedule + run-due tick + audit view → pause → status paused + ProjectPaused event.
  - FLOW 6: portal claim → claimed_unverified → reconciliation totals verifiedPaid 0 / outstanding > 0 → history 200; milestone never paid.
  - FLOW 7: propose 2×$600 plan → portal accept → milestone plan_active → verified seedPayment → mark-paid seq 1 → installment paid, outstanding 120k→60k.
  - FLOW 8: portal dispute → timeline still contains ProjectCreated/PaymentReceived/MilestoneApproved → evidence pack 201 with sha256 → detail consistency.matchesGeneration true.
  - FLOW 9: intruder read/write → 403/404, tampered token → 401, anonymous → 401, own token control → 200.
  - RACE (fresh unpaid milestone): concurrent duplicate webhook → exactly one duplicate:true, PaymentReceived delta exactly 1, payment paid; concurrent same-key checkouts → same payment id, at most one new row.
- FAILURES FOUND AND FIXED (all four were test-sequencing errors, not product bugs — each fix asserts the intended guard): (1) scheduling reminders after funding → 422 is the paid-stop invariant — moved scheduling before confirm_funding + assert the 422; (2) reconciliation body is `{reconciliation:{totals}}` — fixed parsing; (3) portal approve control on a paid-but-unsubmitted milestone → 422 — control is now portal overview 200; (4) checkout on an already-paid milestone → 409 "already paid" — race test now uses a fresh milestone with exact assertions.
- RACE VERDICT: duplicate/concurrent webhook delivery is safe — in-memory fast path + UNIQUE `webhook:stripe:<eventId>` marker make the append atomic (no awaits between check and insert), loser returns duplicate:true. No product change required.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 347/347 tests: 336 pre-existing + 11 new), `npm run build` compiles. No version bump (verification-only session, no product change). No secrets committed. No git push (unattended).
- Files changed — new: `tests/integration/endToEnd.test.ts`; edited: `progress.md` (this entry).
- Best next step: wire the production storage bucket with `Content-Disposition: attachment` + `nosniff` (Session-19 follow-up, still open), then server-side session revocation if abuse appears.

### 2026-09-18 — Session 20 (Senior SaaS product designer — operations hierarchy, v0.18.2)

- Read progress.md + inspected repo (Session-19 audit intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 332 tests green). No architecture change, no random redesign — every edit is presentation-layer hierarchy/copy in `src/ui/components.ts`, `src/routes/pages.ts`, `src/routes/portal.ts` plus one regression test. All API routes, domain rules, migrations and security fixes preserved.
- FREELANCER ("safe / owed / next / automatic"): project page now opens with a money band (Safe-verified-paid / Owed-outstanding / Due-next), an expanded Next-action + due-date line, a 6-column milestone timeline (amount, due, work, payment, approval/delivery, "what you should do"), and an Operations card (agreement, deliverable review, approvals, reminder setup, calm overdue workflow). Project list gains pipeline overview + 4-step starter + "what happens automatically". Clients list, timeline ("How to read this"), evidence ("When to use this": follow-ups → plan → pause → export), protection (Needs-attention first, Clear second) and notifications ("Set up reminders once": due date → dry-run → schedule) all guide next steps. 404s now use errorState with a next step; unauthenticated page explains sign-in is the only step.
- CLIENT ("paying-for / approved / remains / need"): portal now opens with money band + progress bar, "How this works" 4 steps (review → approve → pay → receive), "What happens next", "What you have approved" (empty state included), milestone timeline, "What you have paid" (verified-only + confirming note), agreement, locks, conditional "If you need more time" (payment-plan path, only on overdue/disputed), "What happens automatically" (receipts, unlocks, reminders), updates. Invalid/missing-token pages add "What to do next" (fresh link, nothing lost). Approval copy pins the version; pay copy stays intent-only.
- CALM OPS TONE: statusPill remapped — overdue = amber warn, disputed/rejected = info blue, red reserved for genuine failed delivery only. New primitives: moneyBand, onboardingSteps, automationNote ("Automatic" label), errorState; emptyState gains optional action. CSS adds moneyband/steps/automation/focus-visible + mobile stacking. Swept copy for banned debt-collection phrasing (none present; regression test asserts absence).
- TESTS: new `tests/integration/productDesign.test.ts` (4 tests: freelancer hierarchy + links preserved, timeline/evidence/protection/notifications guidance, portal hierarchy + calm copy, error next-steps). Manual flow verification via inject: project dashboard, timeline, evidence, protection, notifications, portal + bad-token pages. Full `npm run checks` green (typecheck + eslint zero-warnings + prettier + 336/336 tests), `npm run build` compiles. Bumped 0.18.1→0.18.2 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
- Files changed — new: `tests/integration/productDesign.test.ts`; edited: `src/ui/components.ts` (tones + 5 helpers + CSS), `src/routes/pages.ts` (onboarding, dashboard, timeline, evidence, protection, notifications, error states), `src/routes/portal.ts` (hierarchy, approved/remains/automatic, overdue path, error next-steps), `src/routes/health.ts` + `package.json` + `tests/integration/http.test.ts` (0.18.2), `progress.md` (this entry).
- Best next step: wire the production storage bucket with `Content-Disposition: attachment` + `nosniff` (Session-19 follow-up), then consider a freelancer "reminder setup" wizard POST flow if onboarding data shows drop-off.

### 2026-09-18 — Session 19 (Hostile security reviewer — full-application audit, v0.18.1)

- Read progress.md + inventoried the repo (Session-18 notifications intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 325 tests green). Preserved all architecture + session-scoped invariants; added NO product features — every change is a vulnerability fix or a no-behaviour-change hardening fix.
- EXPLOITATION (throwaway `app.inject` probes, deleted after use): confirmed ONE real vulnerability — checkout `idempotencyKey` lookup was global, so attacker workspace B replaying victim workspace A's key got HTTP 200 with the victim's payment row (workspace/project/milestone ids, providerPaymentId, amountCents). Also confirmed `successUrl: javascript:…` accepted (201). Verified as already-safe (re-pinned by tests): cross-tenant IDOR reads (403/404), portal token single-project scope (401 cross-project, incl. timeline), claim → `claimed_unverified` only, success page read-only, unsigned webhook → 400, replay → `duplicate: true` no-op, unpaid finals 423 (freelancer + portal, even with `?file=`), timeline write methods → 405, readonly role blocked from writes/member-mgmt (403), empty/blank release overrides rejected, final object keys never leak pre-release.
- FIXES: VULN-001 checkout duplicate shortcut re-scoped to URL workspace+project, out-of-scope repeats get a bare 409 with no row data (`src/routes/payments.ts`); VULN-002 `successUrl`/`cancelUrl` restricted to http(s) via new `httpUrlSchema` (422 otherwise); VULN-003 portal timeline `loadPortal` gains the `link.workspaceId !== project.workspaceId` check every other portal authorizer has (`src/routes/timeline.ts`); VULN-004 both final-download endpoints resolve strictly within `visibility === "final"` files — no review-file fallback (`src/routes/deliverables.ts`).
- DOCS: new `docs/security-audit.md` (verdict table on all six session attack questions, 4 fixed findings with exploit evidence, probed-and-safe invariants, 9 accepted risks/follow-ups: signup oracle, session revocation, CSRF posture, public-endpoint limits, webhook races, bucket attachment headers, roster PII, packs freelancer-only, in-memory webhook fast path).
- TESTS: new `tests/integration/securityAudit.test.ts` (7 exploit-regression tests: cross-tenant key reuse → 409 with no victim data; non-http(s) URLs → 422 + https/http pass; forged webhook → 400 + replay duplicate + paid applied once; claim/success never pay; cross-project token 401; unpaid-final 423 + finals-only; timeline 405 + tenant isolation). Full suite 332/332 green (325 pre-existing + 7 new).
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 332/332 tests), `npm run build` compiles. Bumped 0.18.0→0.18.1 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
- Files changed — new: `docs/security-audit.md`, `tests/integration/securityAudit.test.ts`; edited: `src/routes/payments.ts` (VULN-001 scoping + `httpUrlSchema`), `src/routes/timeline.ts` (VULN-003 workspace check), `src/routes/deliverables.ts` (VULN-004 finals-only), `package.json` + `src/routes/health.ts` + `tests/integration/http.test.ts` (0.18.1), `progress.md` (this entry).
- Best next step: wire the production storage bucket with `Content-Disposition: attachment` + `nosniff` (audit follow-up #7), then server-side session revocation + signup CAPTCHA if abuse appears.

### 2026-09-18 — Session 18 (Implementation engineer — production-grade notifications, v0.18.0)

- Read progress.md + inspected repo (Session-17 AI drafts intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 302 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals + configurable reminder engine + exact-sum payment plans + chronological evidence timeline + factual evidence packs + 12 observable protection checks + review-required AI drafts; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (invariant preserved — notices are system-voiced workflow records, never threats).
- NOTIFICATION DOMAIN (new pure `src/domain/notifications.ts`, no DB/network): 18 transactional kinds across 7 categories (payments/approvals/reminders/overdue/pauses/plans/deliverables); 18 professional templates (subject + text + HTML, system voice "Automated notice from {workspaceName}", workspace footer with manage-preferences link, informational-record disclaimer) with `assertTransactionalCopy()` banned-phrasing guard (legal threats, client labelling, guarantees, "Deposit" label); stable `notify:<kind>:<scope>:<dedupe>` idempotency keys; paid-stop (`paid/funded/refunded` settle, `disputed` pauses) via `isPaymentSettled()/maySendOverdue()`; exponential backoff `2^attempt` min capped 4h, 5 attempts max; single `shouldDeliver()` preference/opt-out gate; `defaultPreferences()` (absent = enabled).
- DATABASE: extended `prisma/schema.prisma` (Notification `kind`/`category`/`readAt` + `notification_preferences` + `notification_opt_outs` with workspace relations) + new `prisma/migrations/0011_notifications/migration.sql` (columns defaulted, tables + uniques + indexes, backward compatible). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
- STORE: `NotificationRecord` gains `kind/category/readAt` (+ create/update inputs) + new `NotificationPreferenceRecord`/`NotificationOptOutRecord` + 8 methods (`listWorkspaceNotifications/listInAppForUser/get+setNotificationPreference/find+add+remove+listNotificationOptOut`) on `InMemoryStore` (normalized-email opt-outs, category + `all` fallback) and `PrismaStore` (+ tolerant `mapNotificationRow`).
- SERVICE (`src/lib/notify.ts` `NotificationService` + `src/lib/unsubscribe.ts` HMAC tokens, 1y TTL, constant-time verify): idempotent `queue()` (repeats → `duplicate: true`; opt-outs recorded as suppressed/canceled rows, never sent); `queueForScope()` fan-out (one client email with `client:<dedupe>` namespace + one in-app row per member with `inapp:<user>:<dedupe>` namespace, per-member pref checks, suppressed list); `dispatchEmail()` (paid-stop re-check at send time, attempt/error/next-retry bookkeeping, sent-row repeats are duplicates); in-app publishes instantly as `delivered` honouring prefs.
- API (new `src/routes/notifications.ts`, same membership/IDOR gates; ticks/retry/cancel/opt-out-write need write role): inbox `GET .../notifications` (channel/category/state filters + `summary {unreadInapp, failedCount, queuedCount}`) + detail (full `delivery {status, attemptsLeft, nextRetryAt}`) + in-app `POST .../:id/read` (idempotent) + failed-email `POST .../:id/retry` (paid-stop aware) + `POST .../:id/cancel` (queued only) + `GET/PUT .../notification-preferences` (14-cell matrix, you-only) + `GET/POST/DELETE .../notification-opt-outs` (freelancer-managed) + public `GET` (token preview, no state change) + `POST /api/v1/notifications/unsubscribe {token}` (idempotent confirm, masked email) + `POST .../projects/:pid/notifications/dispatch-due` (outbox tick: sends due rows, auto-cancels settled/opted-out) + `POST .../projects/:pid/notifications/check-overdue` (one notice per open milestone per day — repeats are duplicates, never resends; paid/disputed skipped; client email dispatched in-tick with failure deferred to retry).
- LIFECYCLE HOOKS (all via best-effort `notifyLifecycle()`, each appending an idempotent `NotificationQueued` event — notifications never break the workflow): verified-payment webhook (`payment_received/refunded/disputed/failed`, dedupe `webhook:<eventId>`); portal milestone decisions (`approval_received/revision_requested/rejected/disputed`, dedupe per approval); portal deliverable approve (`deliverable_approved`); freelancer release (`deliverable_released`, dedupe per deliverable version); pause/unpause (`project_paused/unpaused`); plan propose/accept (freelancer + portal)/complete/default (`plan_*`). Plan run-due keeps its existing idempotent per-installment reminders (no double-mail); missed-installment mail intentionally not duplicated.
- TIMELINE: new `NotificationQueued` descriptor (reminder category, clientSafe false — portal-safe, no userId leak); updated Session-14 chronological assertion (pause, then its notice) + unit coverage for the descriptor.
- UI (`src/routes/pages.ts`): new `/app/notifications` center (your inbox, preference matrix, opt-out list) + project-page link.
- TESTS: 23 new (9 unit `tests/unit/notifications.test.ts`: kind/category coverage, per-kind banned-phrase sweep + footer/unsubscribe assertions, key stability, paid-stop matrix, backoff caps, gate matrix, defaults; 14 integration `tests/integration/notifications.test.ts`: setup, empty inbox + 14-cell defaults, overdue tick (professional copy + unsubscribe link + kind/category/delivery), repeat-tick no-resend, in-app read idempotency, fail→`failed_retry_scheduled`→retry→sent→duplicate, paid-stop (paid skipped, open milestone still notified exactly once), per-user in-app suppression, pause fan-out (client email queued, member in-app suppressed) + outbox dispatch + unpause, opt-out suppression + public token preview + tampered 400, public confirm + resubscribe, detail + cancel, center page + project link (cookie session), 403/404 isolation). Full suite 325/325 green, including all 302 pre-existing tests (Session-14 timeline assertion updated for the intended `NotificationQueued` tail).
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 325/325 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.17.0→0.18.0 (`package.json`, `APP_VERSION`, http test). README gains a Notifications section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 17 (Implementation engineer — workflow-scoped AI assistance, v0.17.0)

- Read progress.md + inspected repo (Session-16 protection checks intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 286 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals + configurable reminder engine + exact-sum payment plans + chronological evidence timeline + factual evidence packs + 12 observable protection checks; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (invariant preserved — AI outputs are review-required drafts, never verdicts).
- Scope decision (per task): NO chatbot. AI is used only where it materially improves the payment-protection workflow — five helpers in new pure `src/domain/aiAssist.ts` (engine `assistive-v1`, extractive + local + DB-free): (1) contract/terms extraction — 6 fields (payment terms, milestones, deadlines, late-fee language, revision terms, final-delivery conditions) with verbatim quotes + missingFields, never invented; (2) communication extraction — approval / revision request / promised payment date / payment-plan discussion / deliverable acceptance, each with a verbatim quote (+ observed date text when present) and a "confirm via normal flows" note; (3) reminder drafting — professional copy rendered strictly from database facts (amounts/dates loaded by the route, never free text); (4) evidence summarization — chronological dated bullets restating recorded headlines, no conclusions; (5) agreement consistency check — schedule-vs-milestone-vs-total contradictions, per-position amount diffs, deposit mismatch, missing due dates, missing final-delivery condition, missing late-fee terms, or a factual `consistent` finding. `assertAssistCopy()` rejects client labelling (scam/fraud/…), legal threats (sue/lawsuit/court/…), and outcome guarantees; every output carries `reviewRequired: true` + `financialRecordsChanged: false` + engine label + disclaimer.
- API (new `src/routes/aiAssist.ts`, same membership/IDOR gates, read-only/draft-only — no store writes, no events, no mail): `POST .../projects/:pid/ai/extract-terms {sourceText≤20000}` + `POST .../ai/extract-communications {sourceText}` (pasted-text only, project-scoped for tenancy) + `POST .../ai/draft-reminder {milestoneId, tone?, portalUrl?}` (facts from live milestone/project/client/payments rows; verified-only math; sending still requires the manual reminder flow) + `POST .../ai/summarize {maxEvents?≤200}` (restates `describeEvent()` headlines over the recorded trail) + `GET .../ai/consistency?agreementId?` (latest or named agreement vs live milestones + project total). No migration (nothing persisted).
- UI (`src/routes/pages.ts`): project page "Recent activity" links to new `/app/projects/:id/ai` (one card per helper with `data-api-form` POST forms + consistency JSON link + safety-rules card). Same session-cookie membership checks, generic 404s across tenants.
- TESTS: 16 new (8 unit `tests/unit/aiAssist.test.ts`: full-field extraction + quote-verbatim check, no-invention empty case, communication kinds + promised-date capture, facts-only reminder draft + banned-phrase sweep, bullet restatement + actor counts, multi-finding consistency, consistent-report, guard rejections; 8 integration `tests/integration/aiAssist.test.ts`: setup, extract-terms draft envelope, extract-communications with timeline-unchanged proof, draft-reminder with amounts-from-DB + zero-reminders proof, summarize bullet/event-count parity, consistency no-agreement finding, AI page + project-page link (cookie session), 401 + cross-tenant 403/404 + unknown-milestone 404). Full suite 302/302 green, including all 286 pre-existing tests.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 302/302 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set). Bumped 0.16.0→0.17.0 (`package.json`, `APP_VERSION`, http test). README gains an AI drafts section. No secrets committed. No git push (unattended).
- Files changed — new: `src/domain/aiAssist.ts`, `src/routes/aiAssist.ts`, `tests/unit/aiAssist.test.ts`, `tests/integration/aiAssist.test.ts`; edited: `src/config/app.ts` (route wiring), `src/routes/pages.ts` (AI page + project link), `src/routes/health.ts` (0.17.0), `package.json` (0.17.0), `tests/integration/http.test.ts` (0.17.0), `README.md` (AI drafts section), `progress.md` (this entry).

### 2026-09-18 — Session 16 (Implementation engineer — project protection / risk-awareness, v0.16.0)

- Read progress.md + inspected repo (Session-15 evidence pack intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 277 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals + configurable reminder engine + exact-sum payment plans + chronological evidence timeline + factual evidence packs; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (invariant preserved — checks describe workflow conditions only, never legal conclusions).
- PROTECTION DOMAIN (new pure `src/domain/protection.ts`, no DB/network): `buildProtectionChecks()` recomputes 12 observable conditions from live project data — payment method not configured (agreement methods + payment terms), deposit missing (first milestone verified cover), milestone overdue (past due + verified balance), contract unsigned (no accepted agreement), deliverable approved but unpaid, client requested final files before payment (client DeliverableViewed while verified-unpaid), payment deadline unclear (unpaid milestone without due date), multiple payment failures (≥2 failed/cancelled), repeated missed installment dates (≥2 missed rows/events), no recent client activity (>14 days with outstanding, quiet-when-paid is clear), large unpaid balance (≥$500 AND ≥25% of total, verified-only), final deliverable currently unlocked (released/paid deliverable or unlocked milestone state while verified-unpaid). Each check returns `{code, title, status clear|needs_attention, detail, nextStep, evidence}` with ids/dates/amounts; report returns `attentionCount/clearCount/status healthy|needs_attention` + disclaimer — no risk score, no grade, no prediction anywhere. `assertProfessionalCopy()` rejects scammer/scam/bad-client/dishonest/fraudulent/fraud/cheat phrasing.
- API (new `src/routes/protection.ts`, same membership/IDOR gates, read-only): `GET /api/v1/workspaces/:wid/projects/:pid/protection` (report + disclaimer + explicit no-score note). No migration (recomputed from existing tables).
- UI (`src/routes/pages.ts`): project page "Recent activity" links to new `/app/projects/:id/protection` ("Protection checks" / "Project health": status pills, detail, next step per row, JSON link, about-checks card). Same session-cookie membership checks, generic 404s across tenants.
- TESTS: 9 new (6 unit `tests/unit/protection.test.ts`: 12-code coverage + no-score assertion, healthy-all-clear, risky multi-flag with evidence + banned-word sweep, stale-activity-only-when-outstanding, verified-only balances, labelling-guard; 3 integration `tests/integration/protection.test.ts`: fresh-project attention rows + disclaimer + no-score keys + banned-word sweep, Project-health HTML + project-page link (cookie session), 401 + cross-tenant 403/404 + page isolation). Full suite 286/286 green, including all 277 pre-existing tests.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 286/286 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set). Bumped 0.15.0→0.16.0 (`package.json`, `APP_VERSION`, http test). README gains a Project health section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 15 (Implementation engineer — evidence pack, v0.15.0)

- Read progress.md + inspected repo (Session-14 timeline intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 263 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals + configurable reminder engine + exact-sum payment plans + chronological evidence timeline; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (invariant preserved — pack states facts only, never legal conclusions).
- PACK DOMAIN (new pure `src/domain/evidencePack.ts`, no DB/network): `buildEvidencePackSnapshot()` covers every required section when applicable — parties, project info, agreement version + payment terms, milestone structure, invoices/payment records (tiered verified/claimed), deliverables + versions, approvals with factual statements ("On 2026-09-06, the client approved Milestone 1 — Build version 1."), revisions (RevisionRequested/RevisionSubmitted), chronological timeline, reminder history, payment-plan history; financial summary counts ONLY provider-confirmed receipts (claims never reduce outstanding); `assertFactualCopy()` rejects fraud/liable/breach/sue/guarantee phrasing in snapshot AND rendering; stable canonical JSON + sha256 pin; `renderEvidencePackHtml()` clean printable export (same calm stylesheet, print → "Save as PDF" for the PDF copy). Disclaimer + no-guarantee notice on every output.
- STORE: new `EvidencePackRecord/CreateEvidencePackInput` (caller-supplied `generatedAt` so the stored pin matches the hashed bytes exactly) + `create/listByProject/find` on `InMemoryStore` (workspace/project scoping) and `PrismaStore` (+ `mapEvidencePackRow`, BigInt seq range). No migration needed (evidence_packs table + `no_update_evidence` trigger already exist from 0001).
- API (new `src/routes/evidencePacks.ts`, same membership/IDOR gates; generate needs write role): `POST .../projects/:pid/evidence-packs` (201: pack meta + snapshot + sha256 + disclaimer/no-guarantee; appends `EvidencePackGenerated` with packId/sha/eventCount/hashes); `GET .../evidence-packs` (list); `GET .../evidence-packs/:packId` (meta + live-rebuilt snapshot + `consistency {matchesGeneration, reasons}` — the pack's own generation event is excluded from comparison so fresh reads match; later activity reported factually as drift with a regenerate path); `?format=html` returns the printable export as text/html.
- UI (`src/routes/pages.ts`): project page links to new `/app/projects/:id/evidence` (pack list + generate form + PDF guidance) and `/app/projects/:id/evidence/:packId` (integrity card + JSON/printable export links). Same session-cookie membership checks, generic 404s across tenants.
- TESTS: 14 new (6 unit `tests/unit/evidencePack.test.ts`: full-section coverage + factual phrasing, verified-only money math, empty-project tolerance, hash stability/sensitivity, banned-phrase guard incl. full-HTML sweep, printable render; 8 integration `tests/integration/evidencePacks.test.ts`: rich fixture (agreement send→accept, funding, deliverable version→review, portal approve v1 + revision, claim≠paid, reminder schedule, 2-installment plan) → 201 pack with all sections + claim-excluded outstanding + chronological trail, list + fresh-detail match, pause-induced drift, printable HTML with sha + Save-as-PDF, EvidencePackGenerated in timeline, calm UI pages, cross-tenant 403/404 + 401). Full suite 277/277 green, including all 263 pre-existing tests.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 277/277 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set). Bumped 0.14.0→0.15.0 (`package.json`, `APP_VERSION`, http test). README gains an Evidence pack section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 14 (Implementation engineer — evidence timeline, v0.14.0)

- Read progress.md + inspected repo (Session-13 payment plans intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 249 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals + configurable reminder engine + exact-sum payment plans; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (invariant preserved).
- TIMELINE DOMAIN (new pure `src/domain/timeline.ts`, no DB/network): 11 categories (project/agreement/milestone/deliverable/approval/payment/reminder/payment_plan/access/dispute/system) + plain-language headline/detail for every important event (Project created/accepted flows, Milestone created, Payment requested/received, Deliverable uploaded, Preview viewed, Revision requested/submitted, Milestone approved, Payment overdue, Reminder sent, Payment-plan proposed/accepted, Work paused, Final asset unlocked); unknown future types stay visible under `system` (never dropped); chronological `filterTimeline()` (types/category/actor/milestone/date/search); `toClientSafePayload()` allowlisted scalars only (hashes/tokens/provider refs/device fingerprints never leave via portal); `summarizeTimeline()` totals/by-category/by-actor/oldest/newest. No migration needed (events table + `no_update_events` trigger already append-only).
- STORE: new `findProjectEventById` on `InMemoryStore` + `PrismaStore`; `listProjectEvents` cap raised 100→500 for timeline pagination. `src/routes/resources.ts`: `POST .../projects` now appends `ProjectCreated` (title/total/currency; the trail starts at birth); new event-sourced `POST .../projects/:pid/pause {reason?}` → `ProjectPaused` + status `paused` and `POST .../projects/:pid/unpause` → `ProjectUnpaused` + status `active` (pausing never edits history; double-pause/unpause-when-active → 409).
- API (new `src/routes/timeline.ts`, same membership/IDOR gates): freelancer `GET .../projects/:pid/timeline` (chronological oldest-first default; filters `types/category/actorType/milestoneId/from/to/search/limit≤200/cursor/order`; each event carries category/label/headline/detail/actor/milestone/occurredAt/metadata + `prevEventId/nextEventId` + `immutable:true`; response adds `summary` + `pagination` + `categories` + disclaimer) + `GET .../timeline/:eventId` detail; write-shaped methods on both URLs return explicit `405 IMMUTABLE_HISTORY` (history cannot be rewritten — record a new event instead). Portal (magic-link auth, project-scoped 401s): `GET /api/v1/portal/:pid/timeline` (+ `/:eventId` detail) with client-safe rows only (PortalLinkIssued/ProjectCreated/automation/webhook rows hidden; metadata allowlisted).
- UI (`src/routes/pages.ts`): project page "Recent activity" links to new `/app/projects/:id/timeline` (server-rendered, category/actor/search/order filter form, each row links to detail) + `/app/projects/:id/timeline/:eventId` (what-happened + recorded facts + append-only integrity note). Same session-cookie membership checks, generic 404s across tenants.
- TESTS: 14 new (5 unit `tests/unit/timeline.test.ts`: full important-event coverage, unknown-type visibility, chronological multi-filter, client-safe stripping, summary; 9 integration `tests/integration/timeline.test.ts`: ProjectCreated-first trail → pause-last ordering + metadata/summary/disclaimer, category/actor filters, detail with nextEventId, 405 immutability on POST/PATCH/DELETE (+ single-event DELETE), unpause-via-event, portal client-safe view + tampered-token 401 + internals absence, calm HTML list + detail pages, cross-tenant 403/404). Updated `tests/integration/clientProject.test.ts` summary assertion (trail now contains both "Payment requested" and "Project created"). Full suite 263/263 green, including all 249 pre-existing tests.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 263/263 tests), `npm run build` compiles. Bumped 0.13.0→0.14.0 (`package.json`, `APP_VERSION`, http test). README gains an Evidence timeline section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 13 (Implementation engineer — payment-plan support, v0.13.0)

- Read progress.md + inspected repo (Session-12 reminders intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 219 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals + configurable reminder engine; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (plan reminders are system-voiced workflow notices, never threats).
- PLAN DOMAIN (new pure `src/domain/paymentPlans.ts`, no DB/network): `validatePlanProposal()` — 1–12 installments, each ≥1 cent, non-decreasing due dates, amounts sum EXACTLY to the outstanding obligation (covers both task examples: 4×$600 and $1,000+$700+$700 against $2,400; short/excess sums rejected so nothing is silently forgiven or surcharged); `buildSchedule()` stable 1-based seq; `summarizePlan()` paid/remaining/next-due/missed/complete; `installmentView()` derived overdue (never stored silently); guarded `offered→accepted|active→completed|defaulted→superseded` transitions; verified-only `markInstallmentPaid()` (duplicate/unknown-seq/canceled rejected); explicit `markInstallmentMissed()`; `cancelOpenInstallments()` on supersede (paid rows kept); stable `plan:<id>:<transition>` + `plan:<id>:installment:<seq>:<transition>` idempotency keys; `renderInstallmentReminder()` professional copy with schedule facts, banned-phrasing-free. New events in `types.ts`: `PaymentPlanModified/InstallmentPaid/InstallmentMissed/ReminderSent/Completed` (+ existing Offered/Accepted/Defaulted); `events.ts` reducer maps Accepted→plan_active, Completed→paid, Defaulted→overdue, installment/reminder events no-op.
- DATABASE: extended `prisma/schema.prisma` PaymentPlan (`currency`, `version`, `supersedesId` self-reference, `note`, `updatedAt`; `PlanState.superseded`) + new `prisma/migrations/0010_payment_plans/migration.sql` (`PlanState` enum value, columns, `original_amount_cents > 0` CHECK, project index). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
- STORE: new `PaymentPlanRecord/CreatePaymentPlanInput/UpdatePaymentPlanInput` (+ `PaymentPlanInstallmentRecord`) + `create/find/listByMilestone/listByProject/update` on `InMemoryStore` (workspace/project/milestone scoping, positive-obligation guard) and `PrismaStore` (+ `mapPaymentPlanRow/parsePlanInstallments/toPaymentPlanState`; lifecycle-only update mirrors the no-rewrite invariant — obligation/currency/version/lineage write-once, modified schedules are new rows).
- API (new `src/routes/paymentPlans.ts`, same membership/write-role/IDOR gates): `POST .../milestones/:mid/payment-plans` (201; outstanding = milestone − verified paid; 422 when paid/disputed/zero-outstanding/sum-mismatch; 409 while a plan is live unless `supersedesPlanId` names it; supersede marks the old plan `superseded`, cancels its open installments, emits `PaymentPlanModified`); milestone `GET .../payment-plans` + `GET .../payment-plans/:planId` + project `GET .../payment-plans` (every view returns originalObligation [frozen snapshot] + agreedModification [version/state/installments] + currentOutstanding [live verified math] + timeline [plan-scoped events in order]); `POST .../:planId/accept` (freelancer-recorded; offered-only; milestone → plan_active; supersedes sibling offered drafts); `POST .../:planId/installments/:seq/mark-paid {paymentId}` (verified-receipt-only 422 otherwise, currency match, amount covers installment, no double-apply; full settlement → `completed` + milestone `paid`); `POST .../:planId/run-due {now?}` (missed-flagging + one automatic system-voiced reminder per unpaid installment via the email seam with `Notification` audit rows keyed `plan-reminder:<plan>:<seq>:<day>` — repeats skip as duplicates, failures recorded per-row); `POST .../:planId/default {reason≥8}` (active-only; milestone → overdue; obligation stands). Portal (magic-link auth, project-scoped 404s): `GET /api/v1/portal/:pid/payment-plans` (+ detail, disclaimer attached) + `POST .../payment-plans/:planId/accept {token}` (client agreement; offered-only; milestone → plan_active).
- TESTS: 30 new (15 unit `tests/unit/paymentPlans.test.ts`: both task examples, short/excess rejection, empty/oversized/zero/unordered rejection, seq build, paid/remaining/next-due summary, derived-overdue non-mutation, missed counting + idempotent re-miss, paid-cannot-miss + double-pay + unknown-seq, supersede-cancel keeps paid, full state machine incl. illegal jumps, key stability, professional reminder copy with banned-phrasing sweep; 15 integration `tests/integration/paymentPlans.test.ts`: $2,400 setup → 4×$600 propose with three-part truth + Offered timeline → sum-mismatch 422 → portal list + portal accept → milestone plan_active → re-accept 422 → live-propose 409 → run-due missed×4 + 4 reminders + Missed/ReminderSent timeline → idempotent re-run (no resends) → pending-payment 422 → verified mark-paid (outstanding 240k→180k) → modified v2 via supersedes (v1 superseded, obligation snapshot intact, open rows canceled, paid row kept, Modified in timeline) → accept v2 → settle 3/3 → completed + outstanding 0 + milestone paid → re-pay/default 422s → project history v1-superseded/v2-completed → cross-tenant 403 + tampered-token 401). Full suite 249/249 green, including all 219 pre-existing tests untouched.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 249/249 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.12.0→0.13.0 (`package.json`, `APP_VERSION`, http test). README gains a Payment plans section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 12 (Implementation engineer — reminder and escalation engine, v0.12.0)

- Read progress.md + inspected repo (Session-11 approvals intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 192 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery + version-pinned approvals; no DRM built (invariant preserved); no escrow built (invariant preserved); no automatic legal claims built (per task — escalation is manual-only + jurisdiction-aware).
- REMINDER DOMAIN (new pure `src/domain/reminders.ts`, no DB/network): configurable `ReminderPolicy` (version/channel/enabled/1–12 steps/maxPerWeek) — NO universal schedule; `DEFAULT_REMINDER_POLICY` is data matching the task example (T-3 friendly, T+0 due, T+3 first overdue, T+7 firmer, T+14 escalation manual, T+21 work-paused manual); `validateReminderPolicy()` rails (offset range −30..90, unique offsets, escalation/work-paused tones MUST be `requiresManual`, template must exist); `parseReminderPolicy()` (`{}` = defaults) + `resolvePolicy()` (project override → workspace defaults → built-in); `planReminderSchedule()` anchors steps at the milestone due date; 6 editable professional templates (v1, system voice "Automated reminder from {workspace}", `{{var}}` rendering with grammatical `{{jurisdiction}}`, `JURISDICTION_NOTICE` informational-not-legal framing, banned-phrasing guard — no legal threats incl. word-boundary sue/lien/court, no "Deposit" label); stable idempotency keys (`reminder:{milestone}:{step}:{day}`, `reminder-send:{id}`). New `ReminderCancelled` event in `types.ts`.
- DATABASE: extended `prisma/schema.prisma` Notification (templateVersion/subject/bodySnapshot/recipientName/deliveredAt/canceledAt/attemptCount/lastError/nextActionAt/nextActionLabel/trigger/policyStep/policyVersion/idempotencyKey UNIQUE) + Project (`reminderPolicy` JSONB default `{}`) + new `prisma/migrations/0009_reminder_engine/migration.sql` (audit columns, unique idempotency index, `projects.reminder_policy`). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
- STORE: `WorkspaceRecord.reminderDefaults` + `ProjectRecord.reminderPolicy` (+ `UpdateProjectInput.reminderPolicy`, `parseReminderDefaults` row mapper) + `updateWorkspaceReminderDefaults` on both stores; new `NotificationRecord/CreateNotificationInput/UpdateNotificationInput` + `create/findById/findByIdempotencyKey/listByMilestone/listByProject/update` on `InMemoryStore` (idempotency→409, workspace/project scoping) and `PrismaStore` (+ `mapNotificationRow/toNotificationState`).
- EMAIL: `FakeEmailProvider` (sent log + `failNext()` failure seam for retry tests) + `resolveEmailProvider()` (fake in test, noop otherwise); wired via `RouteDeps.emailProvider` in `src/config/app.ts` (injectable for tests).
- API (new `src/routes/reminders.ts`, same membership/write-role/IDOR gates): `GET /api/v1/reminder-templates` (v1 catalogue); workspace `GET/PUT .../reminder-policy`; project `GET/PUT .../reminders/policy` (`null` clears to inheritance, unsafe escalation-without-manual → 422); milestone `POST .../reminders/plan` (dry-run preview, no writes; 422 when no due date and no anchorDate); milestone `POST .../reminders/schedule` (201, one queued row per step, stable idempotency keys → repeats return `duplicates`, chained `nextActionAt/Label` per row, `ReminderScheduled` events; 422 when paid/funded/refunded or disputed or policy disabled); milestone + project `GET .../reminders` (full audit rows `scheduled_at/sent_at/state/recipient/template+version/result_error/next_scheduled_action` + `summary` with `nextScheduledAction` + "system is handling this" handlingNote); `POST .../reminders/:rid/send` (manual path — idempotent duplicate when sent, 422 when canceled, manual-only templates need `acknowledgeManualStep=true`, escalation/work-paused tones additionally need `jurisdiction`; success → state sent + sentAt/deliveredAt/providerMessageId/rendered snapshot + `ReminderSent/Delivered` events; provider failure → state failed + attemptCount/lastError + `ReminderFailed` + 502 "safe to retry"); `POST .../retry` (failed-only); `POST .../cancel` (canceledAt + `ReminderCancelled`, idempotent; sent rows refuse); project `POST .../reminders/run-due` (scheduler tick over due queued rows — manual-only steps ALWAYS skipped as `skippedManual`, paid milestones auto-cancel stale rows, failures recorded per-row, tick continues).
- TESTS: 27 new (12 unit `tests/unit/reminders.test.ts`: default stages, manual-only marking, escalation-without-manual rejection, duplicate-offset/unknown-template rejection, override resolution order, due-date anchoring, disabled-policy empty plan, key stability, jurisdiction rendering, banned-phrasing sweep incl. word-boundary lien-vs-client, custom-template rejection, money/overdue helpers; 15 integration `tests/integration/reminders.test.ts`: setup, template catalogue, default policy, unsafe-policy 422, project override, dry-run plan, idempotent schedule, audit-trail + handling summary, send-once + duplicate-no-resend, fail→502→retry, cancel + send-canceled 422, far-future run-due (auto sent, manual skipped) + jurisdiction-gated manual escalation (no-ack 422, no-jurisdiction 422, success), paid-milestone schedule 422, summary audit trail, cross-tenant 403s). Full suite 219/219 green, including all 192 pre-existing tests untouched.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 219/219 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.11.0→0.12.0 (`package.json`, `APP_VERSION`, http test). README gains a Reminders section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 11 (Implementation engineer — formal client approval, v0.11.0)

- Read progress.md + inspected repo (Session-10 deliverables intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 178 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history + 8-state controlled delivery; no DRM built (invariant preserved); no escrow built (invariant preserved).
- APPROVAL DOMAIN (new pure `src/domain/approvals.ts`, no DB/network): decisions `approved|revision_requested|rejected|disputed` with `validateApprovalInput()` (exactly one pinned version — `versionNo ≥ 1` XOR milestone `versionRef` — plus ≥3-char notes for revision/rejection/dispute so the freelancer always knows what to change); `isApprovalCurrent()` (same pinned version, never mixed subjects), `latestDecision()` / `effectiveDecisionForVersion()` (old-version approvals stay historically true per version), `deriveApprovalEffect()` (only the LATEST decision pinning the CURRENT version authorizes release — later revision/rejection/dispute revokes until fresh approval), `eventTypeForDecision()` / `labelForDecision()` (MilestoneApproved/RevisionRequested/ApprovalRejected/DisputeFlagged + client-safe labels).
- DATABASE: extended `prisma/schema.prisma` Approval (deliverableId?, nullable deliverableVersionId, versionNo?, versionRef?, decision default approved, actorType/actorId, ipHash/uaHash — raw IPs/UAs never stored) + new `prisma/migrations/0008_formal_approvals/migration.sql` (columns + `approvals_version_pin_chk` exactly-one-version CHECK + `approvals_decision_chk` + project/milestone + deliverable indexes; existing `no_update_approvals` append-only trigger unchanged). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
- STORE: new `ApprovalRecord/CreateApprovalInput/ApprovalDecision` + `createApproval/listByDeliverable/listByMilestone/listByProject` on `InMemoryStore` (workspace/project/milestone scoping, exactly-one-version + decision + note validation, deliverable scoping) and `PrismaStore` (+ `mapApprovalRow/toApprovalDecision`).
- API (new `src/routes/approvals.ts`, same membership/IDOR gates): freelancer `GET .../deliverables/:did/approvals` + `GET .../milestones/:mid/approvals` (full trail — who/what/which-version/when/decision/note, hashed device metadata never exposed — plus `effective {isCurrent, isApproved, reason}`). Portal milestone (magic-link auth, project-scoped 404s): `approve` now records an Approval row (`portal:<linkId>`, hashed IP/UA) + rich `MilestoneApproved` event and is idempotent on repeat; `request-revision` records `revision_requested`; NEW `reject` (→ `ApprovalRejected`) + `dispute` (→ `DisputeFlagged`, freezes work/payment). Portal deliverable: `approve {versionNo, note?}` records Approval row (idempotent) + rich event; NEW `revision` (steps status back exactly one review step; paid/released → 422 dispute instead), `reject`, `dispute` (each `{versionNo, note≥3}` + decision event), plus review-safe `GET .../approvals` (decisions without internals). Event vocabulary extended (`ApprovalRejected/ApprovalDisputed` in `types.ts`, reducer + portal allowlist updated).
- STALE-APPROVAL FIX (the core bug): `POST .../versions` now resets `approved/payment_pending/paid/released` → `submitted` with a `RevisionSubmitted` supersession event (approvedVersionNo kept as historical pointer, milestone delivery re-synced); milestone `PATCH currentVersionId` to a non-approved version resets `approvalState` → `pending` with a `RevisionSubmitted` event. The database can no longer say "approved" for a version the client never approved.
- TESTS: 14 new (6 unit `tests/unit/approvals.test.ts`: pinning validation, note rules, cross-version non-match, stale-history preservation, latest-decision authority incl. revocation, event/label mapping; 8 integration `tests/integration/approvals.test.ts`: setup→review, formal approval record + history + event payload + hash non-leak, wrong-version 422, v2 upload resets status while v1 approval stays true + effective stale, fresh v2 approval, revision/reject/dispute note rules + status step-back + events, milestone approve idempotent + history + revision/reject/dispute + dispute freeze, cross-tenant 403 + tampered 401 + missing-token 422). Full suite 192/192 green, including all 178 pre-existing tests untouched.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 192/192 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.10.0→0.11.0 (`package.json`, `APP_VERSION`, http test). README gains an Approvals section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 10 (Implementation engineer — deliverable system, v0.10.0)

- Read progress.md + inspected repo (Session-09 trust layer intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 160 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal + Stripe-behind-seam payments + append-only trust history; no DRM built (per task — honest limits notice instead); no escrow built (invariant preserved).
- DELIVERABLE DOMAIN (new pure `src/domain/deliverables.ts`, no DB/network): 8-state controlled-delivery lifecycle `draft→submitted→preview_available→client_review→approved→payment_pending→paid→released` with guarded transitions; conceptual separation `canClientReview()` (preview/staging early) vs `canClientReceiveFinal()` (released ONLY); `HONEST_LIMITS` notice (browser cannot prevent screenshots/copying — previews are a speed bump, not DRM) surfaced in every preview/lock response; `checkRelease()` requires version-pinned approval + verified provider payment (claims never count) with flagged ≥8-char manual override; web-project staging model `none→staging_live→transfer_pending→transferred` (staging reviewable early, production transfer requires release); signed-URL TTL policy (preview 3600s, final 900s).
- SAFE FILE HANDLING (new `src/lib/artifacts.ts` + `FakeStorageProvider`/`resolveStorageProvider()` in `src/lib/providers.ts`, wired via `RouteDeps.storageProvider` in `src/config/app.ts`): server-minted UUID object keys (never user paths), allowlisted content types, 100 MB / 10-files / 20-links caps, https-only links, 64-hex sha256 checks, at-least-one-content-item versions, TTL clamping (never extend).
- DATABASE: extended `prisma/schema.prisma` (Deliverable += description/status/stagingUrl/stagingTransferState/currentVersionNo/approvedVersionNo/updatedAt; DeliverableVersion += description/files/links/previewText/stagingUrl/content-type/size columns, preview/final refs optional) + new `prisma/migrations/0007_deliverables/migration.sql` (columns + status/staging CHECKs + project index). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
- STORE: new `DeliverableRecord/DeliverableVersionRecord` + `create/listByMilestone/listByProject/find/update` + `createVersion/listVersions/findVersion` on `InMemoryStore` (version auto-increment + UNIQUE→409) and `PrismaStore` (+ `mapDeliverableRow/mapDeliverableVersionRow` with file-entry parsing).
- API (new `src/routes/deliverables.ts`, same membership/write-role/IDOR gates): freelancer create/list/detail + `POST .../versions` (mixed files/links/preview/description validated, objects put via storage seam, `DeliverableVersionCreated` event) + `submit` (needs ≥1 version) + `share-preview`/`mark-review` (syncs milestone `deliverableState`, `DeliverablePreviewShared` carries the honest notice) + `mark-payment-pending` + `mark-paid` (verified receipts only, claims → 422) + `release` (approval + verified-paid gate, `DeliverableReleased` or flagged `ManualReleaseOverride`) + `staging` publish + `transfer-request/complete` + `GET .../files/preview` (review files or links/text/staging fallback + notice) + `GET .../files/final` (423 LOCKED pre-release with lock reason + notice, `DeliverableViewed` logged on success). Client (magic-link auth, project-scoped 404s): `GET .../portal/:pid/deliverables` (+ detail) as review-safe projection (final keys never leak, `finalFileCount` only, honest notice), `GET .../preview` (review-visibility signed URL, 1h), `GET .../final` (423 until released, 15-min signed URL after), `POST .../deliverables/:did/approve {versionNo}` (must pin current version, `MilestoneApproved` idempotent).
- TESTS: 18 new (11 unit `tests/unit/deliverables.test.ts`: full lifecycle, illegal jumps, version-pinned approval, review-vs-final matrix, lock reasons + no-DRM notice, release/override gates, staging separation + client projection, mixed-payload validation + rejections, sanitization/keys/TTL clamps; 7 integration `tests/integration/deliverables.test.ts`: shell + submit-needs-version, unsafe-payload 422s, version→submit→preview→review + freelancer preview/final-423, portal review-safe list (no `source.zip` leak) + preview-3600 + final-423-LOCKED, approve→pending→claim≠paid→release-locked→seed-verified→paid→released→portal-final-900 + milestone sync, staging publish→request→complete→transferred portal note, cross-tenant 403 + tampered 401 + missing-token 422). Full suite 178/178 green, including all 160 pre-existing tests untouched.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 178/178 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.9.0→0.10.0 (`package.json`, `APP_VERSION`, http test). README gains a Deliverables section. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 09 (Implementation engineer — payment trustworthiness, v0.9.0)

- Read progress.md + inspected repo (Session-08 payments intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 137 tests green). Preserved Stripe-behind-seam architecture + lifecycle state machine + hosted-checkout-only + claim-never-paid invariant; no escrow built (per task); no DB migration needed (reconciliation is recomputed, webhook markers reuse the events UNIQUE).
- TRUST DOMAIN (new pure `src/domain/reconciliation.ts`, no DB/network): verification tiers `claimed|initiated|provider_confirmed|settled|failed|reversed` (`verificationTierForPaymentState()` + `verificationLabel()` that never equates claim with paid; `isSettlementProviderEvent()` for `transfer.paid/payout.paid/balance.available` + `settlementNote()` documenting Stripe immediate-capture equivalence); `reconcileProject()` compares internal rows vs verified receipts vs milestone invoices vs project total → totals/verification-counts/per-milestone coverage + 18 mismatch codes (`milestone_total_mismatch, currency_mismatch, orphan_payment, missing_provider_linkage, duplicate_applied_payment, unapplied_verified, overpaid_milestone/project, duplicate-safe partial_uncovered, claimed_without_payment, initiated_without_confirmation, failed_payment, dispute_open, refunded_payment, needs_review, out_of_order`) + `balanced` (no errors); `buildPaymentHistory()` plain-language chronological timeline (claims always "NOT verified", confirmations cite provider + settlement, failures "no money moved, safe to retry", disputes "do not release finals").
- API (`src/routes/payments.ts`, same membership/write-role/IDOR gates): freelancer `POST .../milestones/:mid/claim` (records `claimed_unverified` + `PaymentClaimed`, 200 duplicate on repeat, 422 on paid/funded/refunded/disputed — totals untouched), `GET .../payments/history` (read-only timeline), `GET .../payments/reconciliation` (full report + tiered ledger with `verificationTier/verified/settlement` per payment + history), `GET .../payments/diagnostics` (`balanced` + `errors/warnings/info` + per-milestone + resolve-by-new-event note), `POST .../payments/reconciliation/run` (write-role; project-wide `retrievePayment` read-back over open + confirmed rows, legal transitions applied + receipts re-applied, illegal jumps recorded never forced, per-payment results + fresh report). Webhook hardened: persistent `WebhookReceived` marker (`webhook:stripe:<eventId>` UNIQUE) so replay is a no-op across restarts (in-memory set kept as fast path), and reversals arriving before confirmation keep local state with a "(possible out-of-order delivery …)" reason the run later converges.
- PORTAL (`src/routes/portal.ts`): new client `POST /api/v1/portal/:pid/claim {token, milestoneId, note?}` (magic-link auth, project-scoped 404s; `claimed_unverified` + `PaymentClaimed` as client, idempotent `portal-claim:*`; overview `paidCents` unchanged — claim≠paid end-to-end).
- TESTS: 23 new (13 unit `tests/unit/reconciliation.test.ts`: tier mapping/labels, settlement legs, balanced paid, total-drift, overpaid/orphan/currency, claim warnings, partial + initiated, failed/dispute/unlinked, out-of-order, projection drift, history headlines/ordering; 10 integration `tests/integration/reconciliation.test.ts`: setup, freelancer claim + totals-unchanged + idempotent repeat, history NOT-verified lines, diagnostics severity groups, portal claim + overview-paid-0, restart-surviving replay (single `PaymentReceived`), out-of-order refund→paid convergence + warning, partial→paid coverage + 409 duplicate-charge block, project-wide run with Fake `succeeded`, claim-on-paid 422 + 401/403 authz). Full suite 160/160 green, including all 137 pre-existing tests untouched.
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 160/160 tests), `npm run build` compiles, no migration (nothing to validate beyond 0006). Bumped 0.8.0→0.9.0 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 08 (Implementation engineer — payment integration, v0.8.0)

- Read progress.md + inspected repo (Session-07 portal intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 125 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions + magic-link portal; no escrow built (per task — provider settles directly, raw cards never touch the API).
- PAYMENT ARCHITECTURE (Stripe behind a seam, hosted checkout only):
  - Domain: new pure `src/domain/payments.ts` — lifecycle `created→pending→processing→paid` + `failed|cancelled` + `paid→refunded|disputed` (`received`/`partial` kept as legacy verified aliases); `transitionPayment()` guarded throws, `providerEventToState()` maps Stripe types (checkout.session.completed / payment_intent.succeeded|processing|payment_failed|canceled, charge.refunded/disputed, session.expired), `stripeIntentStatusToState()` for retrieve/reconcile, `validateWebhookMoney()` exact integer-cents + ISO-currency match (mismatch → needs-review, never paid), `EVENT_BY_PAYMENT_STATE` audit map, `PAYMENT_ALLOWED_TRANSITIONS` table.
  - Providers: extended `src/lib/providers.ts` — `PaymentProvider` now `{createPaymentRequest, createCheckoutSession, refundPayment, retrievePayment}`; `StripePaymentProvider` (fetch-based REST, no SDK, metadata paymentId/workspace/project/milestone), `FakePaymentProvider` (deterministic ledger + setStatus test seam), `NoopPaymentProvider`, `resolvePaymentProvider()` (Stripe when `STRIPE_SECRET_KEY` set, Fake in test, Noop otherwise).
  - Database: `prisma/schema.prisma` PaymentState += `created|processing|paid|cancelled` + new `prisma/migrations/0006_payment_lifecycle/migration.sql` (enum values + replaces blanket `no_update_payments` trigger with `guard_payment_immutability()` — money/provider linkage write-once, `state/received_at/raw_webhook_ref` advance, DELETEs rejected). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
  - Store: `PaymentRecord` += `provider/providerPaymentId/currency/idempotencyKey/rawWebhookRef/createdAt` + `CreatePaymentInput.id?` (row id pre-generated so webhook metadata == row id) + `create/findById/findByProvider/findByIdempotencyKey/updateLifecycle` (lifecycle-only seam mirroring the DB guard) on `InMemoryStore` and `PrismaStore` (+ `mapPaymentRow`); `seedPayment` backfills legacy calls. Verified-money sets (`money.ts`, `portal.ts`, `projectView.ts`) now count `paid|received|partial`.
  - API (`src/routes/payments.ts`, wired in `src/config/app.ts` with raw-body JSON parser for HMAC-exact webhooks): freelancer `POST .../milestones/:mid/checkout` (write-role + IDOR, amount ≤ outstanding + currency match, `Idempotency-Key` header/body dedupe → 200 duplicate, `PaymentCreated/Pending` events, milestone at most `payment_pending` — never paid), `GET .../projects/:pid/payments` (ledger + verifiedPaid/outstanding/byState summary + authoritative-note), `GET .../payments/:pid` (detail + auditTrail), `POST .../refund` (verified-paid only via provider, milestone → refunded), `POST .../cancel` (pending only), `POST .../reconcile` (verified retrieve; drift recorded, illegal transitions kept local, paid transitions project the milestone); public `POST /api/v1/webhooks/payments` (fail-closed 502 without secret, 400 on bad signature/shape, 200 + ignore on unknown types/payments, 200 duplicate on replay/same-state/event-conflict, 200 needsReview + `PaymentAmountMismatched` on amount/currency mismatch or illegal transition, otherwise lifecycle transition + auditable event + milestone projection; disputed/refunded project milestones best-effort; release still needs approval separately).
  - Portal (`src/routes/portal.ts`): `POST .../portal/:pid/pay` now returns a hosted `checkoutUrl` + `paymentId` but stays intent-only (portal test's `paidCents` still 0 after pay); new read-only `GET .../portal/:pid/payments/:paymentId/status?token=` (verified poll) and `GET /portal/:pid/success?token=&paymentId=` (explicit "this page proves nothing — only verified receipts do", no state change on GET).
  - TESTS: 12 new (7 unit `tests/unit/payments.test.ts`: happy path, branches, terminal rejection, event/status mapping, exact money validation, verified sets, table keys; 5 integration `tests/integration/payments.test.ts`: checkout idempotency + success-page-never-paid, verified-webhook-paid + milestone projection + replay single-`PaymentReceived`, invalid-signature 400 + mismatch needsReview (pending kept + `PaymentAmountMismatched`), failed/cancelled/disputed audit events, provider refund + reconcile + 401/403 authz + currency/amount 422s + unsigned 400).
  - VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 137/137 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.7.0→0.8.0 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
  - Files changed — see Last Session.

### 2026-09-18 — Session 07 (Implementation engineer — client-facing portal, v0.7.0)

- Read progress.md + inspected repo (Session-06 agreement layer intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 117 tests green). Preserved API-first architecture + 5-dimension milestone model + immutable agreement versions; no escrow/payments built (per task — portal pay is intent-only, verified receipts still decide paid).
- DATABASE: new `prisma/migrations/0005_portal_access/migration.sql` (adds `expires_at`/`revoked_at`/`created_at` to `portal_links` — 0001 only had the hash; existing rows default to 7-day expiry). `prisma validate` valid; `prisma generate` ok. No live DB in this environment (migrations still unapplied).
- STORE: new `PortalLinkRecord` + `CreatePortalLinkInput` + `create/list/findById/findByTokenHash/revoke` on `InMemoryStore` (hash UNIQUE→409, project/workspace scoping) and `PrismaStore` (+ `mapPortalLinkRow`).
- DOMAIN: new pure `src/domain/portal.ts` (`buildPortalView` + `toPortalClient`) — answers what am I buying / cost / paid / due-now / remaining / delivered / needs-approval / next / locked-why with calm headlines ("Milestone 2 is ready for review"); verified `received`/`partial` only, claims never count; locked milestones explain predecessor + "no payment required yet"; events allowlisted to client-safe labels with payloads stripped; agreements surface current version + acceptance guidance; input types carry only `{name, company}` so `client.notes`/billing/IP-UA hashes cannot leak by spread.
- API (`src/routes/portal.ts`, wired in `src/config/app.ts`): freelancer `POST .../portal-links` (session + write-role + IDOR; `ttlHours` 1–720; raw `v1.*` token returned once + `portalUrl`, sha256 stored, `PortalLinkIssued` event), `GET` list (no hashes, `active` flag), `POST :linkId/revoke` (`PortalLinkRevoked`); client `GET /api/v1/portal/:pid/overview?token=` (full safe JSON + disclaimer), `GET /portal/:pid?token=` (same view as calm HTML + `/portal/app.js` for approve/revision/pay/accept forms), `POST .../approve` (submitted/viewed only, version-pinned, `MilestoneApproved` as client with idempotency `portal-approve:*`, unlocks recomputed), `POST .../request-revision {note≥3}` (`RevisionRequested` as client), `POST .../pay` (intent only: unpaid→`payment_pending` via `requestFunding` + `PaymentRequested` as client with `portal-pay:*` idempotency; already-paid→409, locked→422; never sets `paid`), `POST .../agreements/:aid/accept {acceptedBy}` (project-scoped, pending-only, hash re-verified, hashed IP/UA, supersedes older accepted, `AgreementAccepted` as client). Token auth: scope+signature+expiry verified, hash lookup, revocation/expiry checked, workspace match; failures generic 401, foreign milestone/agreement generic 404, cross-workspace freelancer issuance 403.
- TESTS: 8 new (4 unit `tests/unit/portal.test.ts`: review headline + lock reasons, verified-only math, safe activity labels, agreement card + notes non-leak; 4 integration `tests/integration/portal.test.ts`: issue→overview→HTML with billing/notes absence, approve→pay-intent (paid stays 0)→locked-pay 422→revision, portal agreement-accept + forged-milestone 404 + tampered-token 401 + cross-tenant 403, revoke→401 + missing-token 422). Fixed in passing: HTML Deposit-word assertion (agreement terms legitimately mention deposit — narrowed to `50% Deposit` label), missing-token status (422 via Zod boundary, not 400), 7 lint + prettier iterations (optional-chain, return-await, non-null hoisting, unnecessary-conditionals).
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 125/125 tests), `npm run build` compiles, `prisma validate` valid, `prisma generate` ok. Bumped 0.6.0→0.7.0 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 06 (Implementation engineer — payment-terms and agreement layer, v0.6.0)

- Read progress.md + inspected repo (Session-05 milestone engine intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 99 tests green). Preserved API-first architecture + 5-dimension milestone model; no payments built (per task — only terms + acceptance).
- DOMAIN: new pure `src/domain/agreement.ts` — 11-section payment-terms validation (amount, deposit=first-milestone, schedule sums-to-total, paymentDueDays/graceDays, method allowlist, late/pause/delivery/ownership/revision/cancellation windows, Deposit-label rejection in schedule titles), `buildAgreementText()` canonical draft ending with the non-law-firm disclaimer, `hashAgreementTerms()` sha256 over key-sorted canonical JSON, lifecycle `draft→pending_acceptance→accepted→superseded/voided` with `AgreementError` codes, `verifyAgreementHash()` + `sameAgreementContent()` for evidence checks. Disclaimer constant (`AGREEMENT_DISCLAIMER`, v1): configurable business terms, not legal advice, enforceability is jurisdiction-dependent.
- DATABASE: extended `prisma/schema.prisma` Agreement (status, total/deposit/schedule/methods/policies/delivery/ownership/revision/cancellation/customClauses/disclaimerVersion/sentAt/voidedAt) + new `prisma/migrations/0004_agreement_terms/migration.sql` (columns + version/status CHECKs; replaces blanket `no_update_agreements` trigger with `guard_agreement_immutability()` — DELETEs rejected, business-content UPDATEs rejected, acceptance-record rewrites rejected, lifecycle transitions allowed). `prisma validate` valid; `prisma generate` ok.
- STORE: new `AgreementRecord` + `CreateAgreementInput` + lifecycle-only `UpdateAgreementLifecycleInput` (no content-update method exists — mirrors the DB guard) + `create/list/find/findByVersion/updateLifecycle` on `InMemoryStore` (version UNIQUE→409) and `PrismaStore` (+ `mapAgreementRow`).
- API (`src/routes/agreements.ts`, same membership+write-role+IDOR gates): `POST .../agreements` (auto-increment version, hash + termsText built server-side, older versions flip isCurrent=false, `AgreementCreated` event), `GET` list + `GET :aid` (detail includes `auditTrail` filtered from project events + disclaimer on every response), `POST :aid/send` (draft→pending only), `POST :aid/accept {acceptedBy}` (re-verifies stored-terms hash before pinning, records sha256 IP/UA hashes never raw values, supersedes older accepted versions with `AgreementSuperseded` events, `AgreementAccepted` as actorType client), `POST :aid/void` (drafts/sent only, promotes newest survivor to current). No PATCH/PUT by design — corrections are new versions. `src/lib/agreements.ts` Zod boundaries strip unknown keys so lifecycle fields cannot be smuggled. `docs/agreement-terms.md` documents coverage/lifecycle/evidence properties.
- TESTS: 18 new (12 unit `tests/unit/agreement.test.ts`: stable hash + disclaimer text, full lifecycle, accept-before-send/double-accept/send-twice/void-rules rejections, acceptedBy rule, sum/deposit/Deposit-label/range/method validation, hash-change detection, 11-section render; 6 integration `tests/integration/agreements.test.ts`: v1 draft with hash+disclaimer, invalid-terms 422s, send→accept with audit-trail + byte-identical history after acceptance, re-accept/re-send rejection, v2 supersede flow with v1 bytes preserved, cross-tenant 403s). One test-data bug fixed in passing (total override tripped deposit guard first).
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 117/117 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.5.0→0.6.0 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 05 (Implementation engineer — milestone engine, v0.5.0)

- Read progress.md + inspected repo (Session-04 client/project domain intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 72 tests green). Preserved API-first architecture + 3-dimension canonical model; no escrow/payments built (per task — only verified-receipt transitions).
- DOMAIN: new pure `src/domain/milestone.ts` — 5 explicit dimensions, zero booleans: work (draft|in_progress|submitted|viewed|revision_requested|approved|disputed), payment (unpaid|payment_pending|funded|paid|overdue|claimed_unverified|plan_active|refunded|disputed — two paid phases so `draft→payment_pending→funded→…→approved→payment_pending→paid→unlocked` is representable without conflation), approval (none|pending|approved|revision_requested|rejected, version-pinned), deliverable (locked|preview_shared|unlocked_ready|released), unlock (locked|available|unlocked, derived from siblings only). 17 guarded transitions throwing `MilestoneTransitionError` (INVALID_TRANSITION/GUARD_VIOLATION/DUPLICATE_PAYMENT/AUDIT_REQUIRED/SEQUENCE_VIOLATION); `createMilestoneSet()` builds the canonical Discovery $500 → Design $1,000 → Development $1,500 → Launch $1,000 set; `changeMilestoneAmount()` needs audit (reason ≥ 8 chars + actorId) after any verified receipt with history preserved; `validateMilestoneSequence()` (contiguous 0..n−1, one currency, non-decreasing due dates) + `reorderMilestones()` (frozen after funding without audit) + `deriveUnlockStates()` (never skips: N available only when predecessors satisfy unlockPolicy); workflow configs DEFAULT (funding-first, sequential previous_paid) / FLEXIBLE (parallel, open) / STRICT; `toLegacyProjection()` maps funded/paid→paid so events/money/release keep working; `ALLOWED_TRANSITIONS` table rendered in `docs/milestone-workflow.md`.
- DATABASE: extended `prisma/schema.prisma` Milestone (currency, approvalState, deliverableState, unlockState, appliedPaymentIds, amountHistory, approved/currentVersionId, updatedAt, nullable dueDate, UNIQUE(projectId, orderIndex)) + Event.seq `@default(autoincrement())` + new `prisma/migrations/0003_milestone_engine/migration.sql` (defaulted/nullable columns, UNIQUE + amount>0 CHECK, backward compatible). `prisma validate` valid with DATABASE_URL; `prisma generate` ok.
- STORE: extended `MilestoneRecord` + new `Create/UpdateMilestoneInput`, `AppendProjectEventInput`; added `createMilestone/findMilestone/updateMilestone/appendProjectEvent` (orderIndex UNIQUE→409, workspace/IDOR checks, idempotency-conflict on event keys) to `InMemoryStore` + `PrismaStore` (+ `mapMilestoneRow`); `seedMilestone` backward compatible (new fields defaulted).
- API (`src/routes/milestones.ts`, same membership+write-role+IDOR gates): `POST/GET .../projects/:pid/milestones` (Deposit titles rejected, currency must match project, auto orderIndex), `GET/PATCH .../milestones/:mid` (descriptive fields freeze after funding; version linkage stays editable), `POST .../amount` (audited changes), `POST .../transitions` (17 actions; duplicate paymentId→409, illegal→422, each appends an event with `${milestoneId}:${paymentId}` idempotency), `POST .../milestones/reorder` (audited when frozen). Unlocks recomputed across siblings after every mutation.
- TESTS: 27 new (21 unit `tests/unit/milestone.test.ts`: canonical $4,000 set, Deposit/amount/currency/order rejection, full funded→approved→paid→released→unlocked path, revision loop, unlock derivation, approve-from-draft rejection, locked-start rejection, funding-first default vs flexible, payout/release-without-approval rejection, version-mismatch rejection, claim≠paid, duplicate-funding/payout idempotency, partial receipts, dispute freeze, amount-audit rules + history, gap/duplicate/currency/due-date sequence violations, frozen-reorder rules, legacy projection + totals; 6 integration `tests/integration/milestones.test.ts`: 4-milestone create + order + unlocks, Deposit/currency rejection, milestone-1 full lifecycle with 409 double-pay + version gating, skip-ahead 422, amount-without-reason 422 / with-reason 200, frozen reorder 422 + cross-tenant 403s). One route bug fixed in passing (PATCH froze version linkage after funding — relaxed so deliverable versions can advance).
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 99/99 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. Bumped 0.4.0→0.5.0 (`package.json`, `APP_VERSION`, http test). No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 04 (Implementation engineer — client and project domain, v0.4.0)

- Read progress.md + inspected repo (Session-03 auth/ownership intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 62 tests green). Preserved API-first architecture; no re-debate of stack/escrow; no payments built (per task).
- DATABASE: extended `prisma/schema.prisma` (Client += phone/billingEmail/billingAddress/timezone/country/status/updatedAt, kept trustTier/notes; Project += startDate/expectedCompletion/paymentTerms/status/updatedAt) + new `prisma/migrations/0002_client_project_domain/migration.sql` (nullable/defaulted columns, backward compatible, `projects_dates_chk` end ≥ start). Ran `npx prisma generate`; `prisma validate` valid with DATABASE_URL (needs live DB for `migrate dev`, unchanged).
- STORE: extended `ClientRecord/ProjectRecord` + `Create/Update` inputs, added `updateClient/updateProject`, `listMilestones/listPayments/listProjectEvents`, and InMemory test seams (`seedMilestone/seedPayment/seedEvent`). `PrismaStore` maps the new columns (ordered lists, null-clearing PATCH semantics); `InMemoryStore` mirrors it so the suite runs without Docker.
- DOMAIN: new pure `src/domain/projectView.ts` (`buildProjectSummary`) — total / paid (verified `received`/`partial` only; `claimed_unverified` never counts) / outstanding (via `money.projectOutstandingCents`) / progress% / projectStatus / paymentStatus (unpaid|partial|paid|overdue|disputed) / currentMilestone (first unpaid by orderIndex) / calm nextAction / 8-item recentActivity. New shared Zod validation `src/lib/clientProject.ts` (client + project create/update, ISO-2 country, ISO-3 currency, integer cents, end ≥ start, list filters).
- API (`src/routes/resources.ts`, same membership+role+IDOR gates): full client profile on create, `PATCH .../clients/:cid`, list `?search=&status=`, detail now includes its projects + count; project scheduling/terms/status on create, `PATCH .../projects/:pid`, list `?search=&status=&clientId=`, detail includes its client, new `GET .../projects/:pid/summary` (command center: totals + milestones + client; no payment creation).
- UI (calm, no admin-dashboard chrome): reusable `src/ui/components.ts` (layout/nav, stat cards, status pills, progress bar, tables, timeline, forms; external `/app/styles.css` + `/app/app.js` so CSP `styleSrc/scriptSrc 'self'` holds, no inline style/script) + `src/routes/pages.ts` (`/app`, `/app/clients`, `/app/clients/:cid` with edit form, `/app/projects`, `/app/projects/:pid` command center showing total/paid/outstanding, current milestone, next action, project+payment status, progress, milestones, recent activity, edit form; 401 sign-in page, cross-tenant 404s). Wired in `src/config/app.ts`. Bumped 0.3.0→0.4.0 (`package.json`, `APP_VERSION`, http test).
- TESTS: 10 new (3 unit `tests/unit/projectView.test.ts`: empty totals, claim≠paid invariant, overdue+labels; 7 integration `tests/integration/clientProject.test.ts`: full-profile create, country/email rejection, PATCH+search filter, scheduling + backward-date rejection, PATCH + seeded summary math, calm HTML markers + no-dashboard check, cross-tenant 403s on PATCH/summary/pages). Fixed one test bug (read `body.project`, not `body`). Fixed lint/type iterations (exactOptionalPropertyTypes spreads, Prisma generate, session object-args, optional-chain narrowing, no-base-to-string).
- VERIFIED: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 72/72 tests), `npm run build` compiles, `prisma validate` valid (DATABASE_URL set), `prisma generate` ok. No secrets committed. No git push (unattended).
- Files changed — see Last Session.

### 2026-09-18 — Session 03 (Implementation engineer — identity, auth and ownership boundaries, v0.3.0)

- Read progress.md + inspected repo (Session-02 foundation intact: Fastify 5 + Prisma 6 + Zod + Pino + Vitest, 39 tests green). Preserved architecture; no re-debate of stack/escrow.
- DESIGN (rationale): stateless HMAC session tokens (no DB session table → no new migration, horizontal scale; sign-out = discard + cookie clear) + `Store` seam (`InMemoryStore` for tests/dev, `PrismaStore` for prod) so ownership-boundary tests run without Docker Postgres. Three-layer gate on every protected handler: requireAuth → requireMembership(URL workspace) → role check → assertResourceInWorkspace (IDOR guard). Roles owner > member > accountant_readonly support solo → assistant → multi-brand futures.
- Implemented libs: `src/lib/session.ts` (issue/verify `s1.*`, Bearer+cookie extract, HttpOnly/SameSite cookie builders, fail-closed secrets), `src/lib/authz.ts` (requireMembership/requireRole/requireWriteAccess/requireOwner/assertResourceInWorkspace, all generic 403), `src/lib/store.ts` (User/Workspace/Membership/Client/Project records; memory impl + Prisma impl with UNIQUE→409 mapping; email normalization).
- Implemented routes: `src/routes/requestAuth.ts` (requireAuth, publicUser, signup/signin/account zod schemas), `src/routes/auth.ts` (signup creates user+default workspace+session 201; signin generic 401 both cases + lastLogin; signout clears cookie; GET/PATCH /me incl. currentPassword-gated rotation), `src/routes/workspaces.ts` (create/list/get, member list, owner-only invites with 404-only-after-authz + 409 duplicates), `src/routes/resources.ts` (tenant clients/projects CRUD-read with write-role gate + cross-workspace-client-link rejection). Wired into `src/config/app.ts` via injectable `store` option; stricter per-route rate limit on signup/signin. Bumped version 0.2.0→0.3.0 (`package.json`, health `APP_VERSION`, `app.ts` root). Updated `README.md` (new routes/libs + Identity section).
- Wrote 23 new tests: `tests/unit/session.test.ts` (6: round-trip, tamper, expiry/wrong-secret, fail-closed, Bearer/cookie extract, cookie flags) + `tests/integration/auth.test.ts` (17: signup/duplicate/weak-password, signin oracle-parity, 401 control, Bearer+cookie /me, displayName update, password rotation + old-password rejection, signout cookie clear, two-tenant isolation 403s, IDOR probes both directions, cross-workspace project-link rejection, multi-brand workspaces, owner/member/accountant_readonly matrix, outsider/duplicate/unknown invite handling).
- Fixed in passing: pre-existing 1-in-16 flake in `tests/unit/magicLink.test.ts` ("rejects tampered signatures" appended "0" which is a no-op when sig already ends in 0) → deterministic digit-flip.
- Verified: `npm run checks` green (typecheck + eslint zero-warnings + prettier + 62/62 tests), `npm run build` compiles, `npx prisma validate` valid (with DATABASE_URL set; schema unchanged), live smoke of `dist/server.js` (listens on :3000). No secrets committed. No git push (unattended).

### 2026-09-18 — Session 02 (Implementation engineer — production foundation, v0.2.0)

- Validated Session-01 docs against empty repo (only docs/ + progress.md + git); confirmed stack unselected → made selection below.
- STACK DECISION (rationale): API-first Fastify 5 + TS strict instead of full-stack Next.js for the foundation, because the Session-02 task is data/event/security correctness, not UI; a lean API keeps domain rules DB-free and inject-testable, and Next.js (or any frontend) can consume it later without rework. Prisma 6 over Drizzle/raw SQL: typed migrations, solo-founder ergonomics, UNIQUE-based idempotency. Zod everywhere (env/HTTP/webhooks/domain) for one validation style. Pino (Fastify-native) + Vitest (fast, TS-native). Postgres 16 (append-only triggers, money integrity) + Stripe-behind-interface + provider seams with Noop impls (dev/test run credential-free). Boring technology throughout; no framework novelty.
- Scaffolded: `package.json` (scripts: dev/build/start/test/typecheck/lint/format/checks/prisma:*), `tsconfig.json` (strict + exactOptionalPropertyTypes + noUncheckedIndexedAccess), `vitest.config.ts`, `eslint.config.mjs` (typed rules for src, zero warnings), `.prettierrc`, `.gitignore`, `.env.example` (placeholders only), `docker-compose.yml` (Postgres 16), `.github/workflows/ci.yml`, `README.md`.
- Implemented config/lib: `src/config/env.ts` (fail-closed zod env), `src/config/app.ts` (helmet+rate-limit+error envelope), `src/server.ts` (boot), `src/lib/{logger,errors,validate,tenant,magicLink,webhook,auth,providers}.ts`, `src/db/prisma.ts` (singleton + `checkDatabase()`), `src/routes/health.ts` (`/health` with up/down DB split, `/api/v1/disclaimer`).
- Implemented typed domain: `src/domain/{types,money,events,release,trust}.ts` — 3-dimension states, 34-event vocabulary, integer-cents math, event reducer (claim≠paid, approval≠paid≠released, refund-keeps-release, occurred_at ordering, unknown-type forward-compat), `canRelease()` pure gate (version-pinned approval, partial/claim block, all-paid mode, flagged manual override ≥8-char reason), trust-ladder defaults.
- Implemented persistence: `prisma/schema.prisma` (User, Workspace, WorkspaceMember incl. `mfaSecret` hook, Client, Project, Milestone with cached projection columns, Agreement versioned, Payment immutable+idempotent, Deliverable+DeliverableVersion, Approval version-pinned NOT NULL, Event append-only, Notification, EvidencePack snapshot, PaymentPlan, PortalLink tokenHash) + `prisma/migrations/0001_foundation/migration.sql` (DDL + `prevent_history_mutation()` triggers on events/payments/approvals/agreements/evidence_packs).
- Wrote 39 tests: `tests/unit/{money,events,release,tenant,magicLink,webhook,foundation}.test.ts` + `tests/integration/http.test.ts` (health envelope, 404 envelope+requestId, helmet headers, disclaimer copy; degraded `db:"down"` path without live DB).
- Ran all checks green: `npm run checks` (typecheck+lint+format+test) — 8 files/39 tests pass; `npx prisma validate` valid; `npm run build` compiles; live smoke test of `dist/server.js` (`/health` → `{"status":"ok","version":"0.2.0","db":"down"}` over real HTTP, no Postgres running).
- Did NOT build UI screens (per task). No secrets committed. No git push (unattended).

### 2026-09-17 — Session 01 (Lead product architect — PRD, no code)

- Inspected repo: confirmed empty directory (no files, no git, no progress.md). Stack: git 2.55, node v24.6.0, npm 11.5.1 available.
- Created `docs/product-requirements.md` v0.1.0: thesis, users (freelancer primary, client secondary), 7 JTBD, 18 MVP capability areas, out-of-MVP list, functional reqs FR-1..FR-36, normative 3-dimension state model, trust ladder, UX principles, NFRs, provider deps, legal boundaries, edge/failure/abuse analysis, MVP-vs-future, open questions.
- Created `docs/domain-model.md` v0.1.0: events-as-truth, entities (User, Workspace, Client, Project, Milestone, Agreement-versioned, Payment-immutable, Deliverable+Version, Approval-version-pinned, Event-append-only, Notification, EvidencePack, PaymentPlan), canonical event vocabulary, transitions, money-integrity rules (minor units), release gates, trust mapping.
- Created `docs/user-flows.md` v0.1.0: Flows 0–10 (onboard, project+agreement, approval, payment→release, late→pause, plan, delivery, evidence export, freelancer dashboard, client portal, trust progression) + failure/abuse paths.
- Created `docs/security-principles.md` v0.1.0: tenant isolation, magic-link rules, webhook verify+idempotency, append-only guards, signed-URL gating, abuse matrix, MVP security checklist, disclaimer requirements.
- Created this `progress.md` per required structure.
- Initialized git + committed (see Last Session).

## Current Database Model

Implemented in `prisma/schema.prisma` + migrations (`0001_foundation`, `0002_client_project_domain`, `0003_milestone_engine`, `0004_agreement_terms`, `0006_payment_lifecycle`, `0007_deliverables`, `0008_formal_approvals` — Sessions 04/05/06/08/10/11, not yet applied to a live DB):

- User (scrypt/bcrypt-compatible `passwordHash` JSON, `mfaSecret` nullable hook, oauth_subject)
- Workspace (+ WorkspaceMember roles owner|member|accountant_readonly; tenant boundary) + `reminderDefaults`
- Client (trust_tier low|standard|high default low; UNIQUE workspace+email; Session 04 += phone, billingEmail, billingAddress, timezone, country, status active|inactive|archived default active, updatedAt)
- Project (currency, total_value_cents, trust_tier_override, pause_state, agreement_version_id; Session 04 += startDate, expectedCompletion with `projects_dates_chk` end ≥ start, paymentTerms, status draft|active|on_hold|completed|cancelled default active, updatedAt)
- Milestone (order_index, amount_cents, due_date nullable since 0003, work_state + payment_state cached projections, release_gate; Session 05 += currency, approval_state, deliverable_state, unlock_state, applied_payment_ids, amount_history, approved/current_version_id, updatedAt, UNIQUE(projectId, orderIndex), amount>0 CHECK)
- Agreement (versioned UNIQUE project+version, is_current, hash, acceptance + ip/ua hash fields; Session 06 += status lifecycle draft|pending_acceptance|accepted|superseded|voided with CHECK, structured business terms — total/deposit/schedule/methods/late/pause/delivery/ownership/revision/cancellation/customClauses/disclaimerVersion/sentAt/voidedAt — guarded by `guard_agreement_immutability()` instead of the blanket no-update trigger)
- Payment (immutable linkage via `guard_payment_immutability()` since 0006: provider+provider_payment_id UNIQUE, idempotency_key UNIQUE, amount/currency write-once; lifecycle `created|pending|processing|paid|received|partial|refunded|disputed|failed|cancelled` advances `state/received_at/raw_webhook_ref` only, reverses via refunded state; every transition appends a `Payment*` event)
- Deliverable (+ DeliverableVersion UNIQUE deliverable+version_no, preview/final refs optional since 0007 + mixed files/links/previewText/staging payload + content-type/size columns; deliverable lifecycle status `draft→…→released` with CHECK + staging transfer state with CHECK + current/approved version pointers)
- Approval (Session 11: append-only decision event — deliverableId?, nullable deliverableVersionId, versionNo XOR versionRef pinning, decision approved|revision_requested|rejected|disputed with CHECKs, approverRef + actorType/actorId, ipHash/uaHash sha256-only; immutable via trigger)
- Event (BIGSERIAL seq, actor, type, payload jsonb, occurred_at UTC, idempotency_key UNIQUE; UPDATE/DELETE rejected by trigger; seq now Prisma `@default(autoincrement())` so app inserts omit it)
- Notification (channel/state machine, scheduling index)
- EvidencePack (snapshot per generation, agreement hashes, seq range, sha256, disclaimer_version)
- PaymentPlan (original-obligation snapshot write-once + currency/version/supersedesId/note/updatedAt; installments jsonb with per-row status; state machine offered|accepted|active|completed|defaulted|superseded) + PortalLink (tokenHash UNIQUE, expiry, revoke)
  Migrations NOT yet applied to a live DB (no Postgres in this environment); apply with `docker compose up -d db` then `npx prisma migrate dev`. Content-guard triggers enforce append-only at the DB layer for events/payments/approvals/evidence (blanket) and agreements (content/acceptance-immutable, lifecycle transitions allowed).

## Important Decisions

- Model events, not just current status: `MilestoneSubmitted/Viewed/RevisionRequested/RevisionSubmitted/Approved/PaymentRequested/Received/DeliverableReleased` etc. are source of truth; status columns (if any) are projections. Reason: evidence timeline is the eventual moat — "what happened", not "current DB value".
- Approval and payment are separate states; delivery is a third dimension. Never collapse to `status="approved"`. Approval ≠ paid ≠ released.
- Trust ladder required: low (small Milestone 1, strict preview-only) → standard → high (Net 30/45, relaxed). Never force "50% deposit or nothing". Downgrade on missed payment with logged reason.
- First-payment UX framing: "Milestone 1 — Discovery & Initial Build — $500", never "Deposit". Progress, not suspicion. Forbid "Deposit" label in default templates.
- Two dashboards: freelancer attention-first (Outstanding / Needs Attention / Overdue / In Progress + money-at-risk sorting + per-project financial card) and client single-next-step portal (Paid/Remaining, current milestone, one CTA, upcoming).
- Differentiator: provable financial state at every lifecycle point (Contract/Money/Work/Delivery/Client/Timeline/Evidence card). Not AI, not invoices, not contracts.
- No self-built escrow / money-transmission in MVP (licensing + risk). Use established provider (Stripe candidate). Never promise guaranteed payment.
- Only verified provider webhooks write `PaymentReceived/Refunded`. `PaymentClaimed` (client assertion) stays `claimed_unverified`.
- Final deliverables cannot bypass release conditions except explicit `ManualReleaseOverride(reason)` flagged in evidence. Previews watermarked/view-only; finals private + signed URLs.
- Amounts in integer minor units + ISO currency; UTC timestamps; UUIDs in URLs (no enumeration).
- Stack intentionally UNDECIDED in Session 01 (docs-only per task). DECIDED Session 02: Fastify 5 API-first + Prisma 6 + Postgres 16 + Zod + Pino + Vitest (rationale in Completed §2026-09-18). Frontend deferred to a later session; no rework needed since domain/HTTP seams are stable.
- Architectural principles above must persist; fresh agents must not rediscover "why no escrow" — it is licensing/risk, recorded here.

## Invariants

These must NEVER be violated:

- Historical payment records are immutable (corrections via new reversing rows).
- Verified provider events determine payment state (client claims never mark paid).
- Tenants cannot access one another's resources (workspace_id scoping + tests).
- Approval is version-specific (old-version approval never releases new version).
- Final deliverables cannot bypass configured release conditions (except flagged manual override with reason).
- Historical evidence cannot be silently rewritten (append-only events; no UPDATE/DELETE from app role).

## Known Issues

- No live Postgres in this environment: migrations validate (`prisma validate` passes with DATABASE_URL, `prisma generate` ok) but `prisma migrate dev` against a real DB has NOT run yet (next session with Docker: `docker compose up -d db` then migrate + smoke `/health` expecting `db:"up"`). `PrismaStore` milestone/event/agreement mappings (Sessions 05–06: milestone columns + orderIndex UNIQUE→409, event idempotency→409 + seq default, agreement version UNIQUE→409 + lifecycle-only updates) are implemented against the schema but integration-proven only via `InMemoryStore`; run one live-DB pass (signup → workspace → client → project → milestones → transitions → reorder → agreement draft → send → accept → v2 supersede + cross-tenant 403 probes) once Postgres is up.
- Sessions are stateless (no server-side revocation list yet): sign-out discards client-side. Add token version/denylist + rotation on password change when threat model demands it.
- ESLint type-aware rules cover `src/` only; `tests/` use base rules (flat-config project scoping). Acceptable: tests are assertion-heavy and Vitest type-strips anyway.
- Auth rate limits are per-route caps (30/min signup/signin) on top of the global limiter; no CAPTCHA/account-lockout yet (brute-force follow-up).

## Security Concerns

- Session-03 checklist status: signup/signin/signout/session-cookie+Bearer DONE (integration-tested); generic-401 credential oracle parity DONE; per-route auth rate limits DONE; workspace membership model (owner/member/accountant_readonly) DONE; server-side authorization on every protected handler DONE; IDOR probes (both smuggling directions + cross-workspace client linkage) DONE (integration-tested, generic 403); account settings + currentPassword-gated rotation DONE.
- Still open: signed-URL gating test (needs storage seam wiring + release integration), live-DB trigger proof + live-DB PrismaStore pass, session revocation list, login lockout/CAPTCHA, SPF/DKIM/DMARC ops, GDPR-anonymization rule (counsel).

## Product Questions

1. Refund/chargeback post-release UX prominence?
2. GDPR erasure vs evidence immutability exact rule (current direction: anonymize PII, preserve facts+hashes)?
3. Agreement template jurisdictions first + counsel review?
4. PPV/"prevented" methodology without overclaim?
5. MVP file size/type limits for previews/finals?
6. Email-only reminders MVP confirmed? SMS later?
7. Single-currency MVP first?
   (Also in PRD §15.)

## Technical Debt

- None blocking. Accepted items: migrations not yet applied to live DB (needs Docker Postgres: `0001_foundation` + `0002_client_project_domain` + `0003_milestone_engine` + `0004_agreement_terms` + `0006_payment_lifecycle` + `0007_deliverables` + `0008_formal_approvals` + `0009_reminder_engine` + `0010_payment_plans`); `PrismaStore` milestone/event/agreement/payment/deliverable/approval/notification/payment-plan mappings need one live-DB pass (covered by InMemoryStore tests meanwhile); sessions stateless without revocation list (documented follow-up); storage/email seams unwired to real providers (Fake/Noop); ESLint type-aware limited to src/.

## Environment

Required variables (wired + fail-closed in `src/config/env.ts`; placeholders in `.env.example`):

- `DATABASE_URL, SESSION_SECRET (32+ chars), APP_BASE_URL` (required — boot throws without them)
- Optional: `STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STORAGE_* (endpoint/bucket/access/secret), EMAIL_* (provider/key/from), MAGIC_LINK_TTL_HOURS (default 168), PORT (3000), LOG_LEVEL, NODE_ENV`
  Commands (all working, verified Session 02):
- install: `npm install`
- dev: `npm run dev` (tsx watch; needs `.env` + optional `docker compose up -d db`)
- test: `npm test` (Vitest: 263 tests incl. tenant-isolation, webhook-idempotency, append-only content, claim-not-paid, version-pinned-approval, session tokens, signup/signin/signout, IDOR probes, role matrix, client/project domain + command-center summary + calm pages, milestone engine domain + API guards, agreement terms domain + version/acceptance API, portal view domain + magic-link issuance/overview/actions, payment lifecycle domain + hosted-checkout/webhook/reconcile API, trust reconciliation domain + history/diagnostics/claims/out-of-order/partial/run API, deliverable lifecycle domain + review-vs-final/staging/safe-handling + gated-release API, formal approval audit domain + stale-reset + revision/reject/dispute + history API, reminder policy/template/schedule/send/retry/cancel/run-due/paid-stop API, payment-plan exact-sum/schedule/accept/portal-accept/missed/auto-reminders/verified-settlement/modified-versions/three-part-truth API, evidence-timeline categories/headlines/filtering/detail/portal-safe/immutable/pause-unpause API)
- lint: `npm run lint` (ESLint typed, zero warnings)
- typecheck: `npm run typecheck` (tsc strict --noEmit)
- build: `npm run build` (tsc → dist/) + `npm start`
- all: `npm run checks`; prisma: `npx prisma validate|generate`, `npx prisma migrate dev`
  Toolchain observed 2026-09-17: git 2.55.0.windows.5, node v24.6.0, npm 11.5.1. Installed Session 02: fastify 5, prisma 6.19.3, zod 3, pino 9, vitest 3.2.7, typescript 5.7, eslint 9.

## Last Session

What was done:

- Session 15 evidence pack (v0.15.0): pure `src/domain/evidencePack.ts` (factual snapshot for overdue/disputed projects — parties/project/agreement+terms/milestones/tiered payments/deliverables/version-pinned approvals/revisions/timeline/reminders/payment-plans; verified-only money; `assertFactualCopy()` banned-phrase guard; stable canonical JSON + sha256; printable HTML with Save-as-PDF guidance; disclaimer + no-guarantee on every output); no migration (evidence_packs table + `no_update_evidence` trigger already exist); store `create/listByProject/find` (memory + Prisma + caller-supplied generatedAt so pins match hashed bytes); new `src/routes/evidencePacks.ts` (201 generate + list + detail with factual drift reporting + `?format=html` printable export; write-role generate; own-generation-event exclusion so fresh reads match); `/app/projects/:id/evidence` list+generate page + `.../evidence/:packId` detail page + project-page link. 14 new tests. `npm run checks` green (277/277), build compiles, prisma validate ok.
- Session 14 evidence timeline (v0.14.0): pure `src/domain/timeline.ts` (11 categories, plain-language headline/detail for every important event incl. Project created/accepted flows, Milestone created, Payment requested/received, Deliverable uploaded, Preview viewed, Revision requested/submitted, Milestone approved, Payment overdue, Reminder sent, Payment-plan proposed/accepted, Work paused, Final asset unlocked; unknown future types visible under `system`; chronological types/category/actor/milestone/date/search filtering; allowlisted client-safe payloads; trail summary); no migration (events table + `no_update_events` trigger already append-only); store `findProjectEventById` (memory + Prisma) + `listProjectEvents` cap 100→500; `POST .../projects` now appends `ProjectCreated`; event-sourced pause/unpause routes (`ProjectPaused/ProjectUnpaused`, 409 on double transitions); new `src/routes/timeline.ts` (freelancer chronological list w/ metadata + prev/next + summary + pagination + disclaimer, single-event detail, explicit `405 IMMUTABLE_HISTORY` on all write-shaped methods; portal client-safe list + detail with magic-link auth); `/app/projects/:id/timeline` filter page + `.../timeline/:eventId` detail page + project-page link. 14 new tests. `npm run checks` green (263/263), build compiles.
- Session 13 payment-plan support (v0.13.0): pure `src/domain/paymentPlans.ts` (exact-sum schedule validation covering both task examples, 1–12 installments, seq assignment, paid/remaining/next-due/missed/complete summaries, derived overdue, guarded offered→active→completed|defaulted→superseded transitions, verified-only mark-paid, explicit mark-missed, supersede-cancel keeps paid rows, stable idempotency keys, professional system-voiced reminders; 5 new `PaymentPlan*` events + reducer mapping); `0010_payment_plans` migration (currency/version/supersedesId/note/updatedAt, `PlanState.superseded`, positive-obligation CHECK, project index); store payment-plan seam (memory + Prisma + mappers + clear); new `src/routes/paymentPlans.ts` (freelancer propose/detail/accept/mark-paid/run-due/default + milestone/project history + portal list/detail/accept; every view shows original obligation + agreed modification + current outstanding + timeline; plans never reduce what is owed until verified paid; modified schedules are new versions, originals never edited). 30 new tests. `npm run checks` green (249/249), build compiles, prisma validate/generate ok.
- Previous: Session 12 reminder + escalation engine (v0.12.0): pure `src/domain/reminders.ts` (configurable policies — project override → workspace defaults → DEFAULT T-3/T+0/T+3/T+7/T+14/T+21 — validation rails incl. manual-only escalation tones, due-date-anchored scheduling, 6 system-voice templates with jurisdiction-aware escalation framing + banned-phrasing guard, `{{var}}` rendering, stable idempotency keys; `ReminderCancelled` event); `0009_reminder_engine` migration (notification audit columns + idempotency unique index + `projects.reminder_policy`); store notification + policy seam (memory + Prisma + mappers + clear); `FakeEmailProvider` + `resolveEmailProvider()` wired via `RouteDeps.emailProvider`; new `src/routes/reminders.ts` (template catalogue, workspace/project policy GET/PUT, milestone plan/schedule, milestone/project audit views + handling summary, send/retry/cancel, run-due tick that never auto-sends manual steps, paid-stop guard). 27 new tests. `npm run checks` green (219/219), build compiles, prisma validate/generate ok.
- Previous: Session 11 formal approval (v0.11.0): pure `src/domain/approvals.ts` (decisions approved/revision_requested/rejected/disputed, versionNo-XOR-versionRef pinning, note rules, isApprovalCurrent/latestDecision/effectiveDecisionForVersion/deriveApprovalEffect, event/label mapping); `0008_formal_approvals` migration (nullable deliverableVersion FK, deliverableId/versionNo/versionRef/decision/actor/ipHash/uaHash columns, exactly-one-version + decision CHECKs, indexes; append-only trigger unchanged); store Approval seam (memory + Prisma + mapper + clear); new `src/routes/approvals.ts` (freelancer deliverable/milestone history + effective); portal milestone approve/revision now write Approval rows + rich events (idempotent approve) + NEW reject/dispute; portal deliverable approve now writes Approval rows (idempotent) + NEW revision/reject/dispute + client-safe history; stale-approval reset on new version (deliverable → submitted + RevisionSubmitted; milestone PATCH → approval pending + RevisionSubmitted); event vocabulary + reducer + portal allowlist extended (ApprovalRejected/ApprovalDisputed). 14 new tests. `npm run checks` green (192/192), build compiles, prisma validate/generate ok.
- Previous: Session 10 deliverables (v0.10.0): pure `src/domain/deliverables.ts` (8-state lifecycle draft→…→released, `canClientReview()` vs `canClientReceiveFinal()`, `HONEST_LIMITS` no-DRM notice, `checkRelease()` approval + verified-paid gate with flagged override, staging `none→staging_live→transfer_pending→transferred`, preview/final TTL policy); safe handling `src/lib/artifacts.ts` (UUID keys, type/size/count caps, https-only links, sha256 checks) + `FakeStorageProvider`/`resolveStorageProvider()` wired via `RouteDeps.storageProvider`; `0007_deliverables` migration (lifecycle/staging columns + CHECKs, optional preview/final refs); store deliverable/version seam (memory + Prisma + row mappers); full `src/routes/deliverables.ts` (freelancer create/versions/submit/share-preview/mark-review/mark-payment-pending/mark-paid/release/staging/transfer + preview/final signed downloads with 423 LOCKED pre-release; client portal list/detail/preview/final/approve with review-safe projection); 18 new tests. `npm run checks` green (178/178), build compiles, prisma validate/generate ok.
- Previous: Session 09 payment trust (v0.9.0): pure `src/domain/reconciliation.ts` (verification tiers claimed/initiated/provider_confirmed/settled/failed/reversed + settlement notes, `reconcileProject()` with 18 mismatch codes + `balanced`, `buildPaymentHistory()` human-readable timeline); freelancer `.../milestones/:mid/claim` (claimed≠paid) + `.../payments/history` + `.../payments/reconciliation` (tiered ledger) + `.../payments/diagnostics` (errors/warnings/info) + `POST .../payments/reconciliation/run` (project-wide provider read-back, illegal jumps recorded never forced); client `POST /api/v1/portal/:pid/claim` (assertion only, totals unchanged); webhook persistent `WebhookReceived` replay markers (restart-safe) + out-of-order reversal notes; 23 new tests. `npm run checks` green (160/160), build compiles, no migration needed (reports recomputed, markers reuse events UNIQUE).
- Previous: Session 08 payment integration (v0.8.0): pure `src/domain/payments.ts` lifecycle (`created→pending→processing→paid` + failed/cancelled + paid→refunded/disputed, exact money validation, provider-event mapping, audit table); provider seam (`StripePaymentProvider` fetch-based + `FakePaymentProvider` ledger + Noop + resolver); `0006_payment_lifecycle` migration (enum expansion + `guard_payment_immutability()` lifecycle guard replacing the blanket no-update trigger); store payment lifecycle seam (memory + Prisma + row mapper, id-aware create, lifecycle-only update); full `src/routes/payments.ts` (freelancer checkout/list/detail/refund/cancel/reconcile + public verified webhook with replay/mismatch/unauthorized coverage); portal pay now returns hosted `checkoutUrl` but stays intent-only + read-only status/success pages that never mark paid; verified-money sets count `paid`; 12 new tests. `npm run checks` green (137/137), build compiles, prisma validate/generate ok.
- Previous: Session 07 client portal (v0.7.0): `0005_portal_access` migration (portal expiry/revocation columns); store PortalLink seam (memory + Prisma); pure `src/domain/portal.ts` client-safe projection (totals/paid/due/remaining, focus headlines, lock explanations, allowlisted activity, agreement card; notes/billing never accepted); full `src/routes/portal.ts` (freelancer issue/list/revoke + client overview JSON + calm `/portal/:pid` HTML + approve/revision/pay-intent/agreement-accept with scope+expiry+revocation checks, foreign-ID 404s, claim-never-paid); 8 new tests. `npm run checks` green (125/125), build compiles, prisma validate/generate ok.
- Previous: Session 06 agreement layer (v0.6.0): 11-section payment-terms validation + canonical draft rendering + sha256 fingerprints in pure `src/domain/agreement.ts`; `0004_agreement_terms` migration (structured term columns, status CHECK, `guard_agreement_immutability()` trigger — content/acceptance immutable, lifecycle allowed); store `create/list/find/findByVersion/updateLifecycle` (memory + Prisma, no content-update method); full agreement API (versioned drafts + send/accept/void, hash re-verified before pinning, hashed IP/UA refs, supersede flow, auditTrail on detail, disclaimer on every response, no PATCH by design); `docs/agreement-terms.md`; 18 new tests. `npm run checks` green (117/117), build compiles, prisma validate/generate ok.
- Previous: Session 05 milestone engine (v0.5.0): 5-dimension explicit states (work/payment/approval/deliverable/unlock, no booleans) + 17 guarded transitions + DEFAULT/FLEXIBLE/STRICT workflow configs + audited amount changes + sequence/unlock guards in pure `src/domain/milestone.ts`; `0003_milestone_engine` migration (milestone currency/state/idempotency/audit columns, nullable dueDate, UNIQUE(projectId, orderIndex), amount>0 CHECK; Event.seq autoincrement default); store `create/find/updateMilestone + appendProjectEvent` (memory + Prisma); full milestone API (CRUD + amount + transitions + reorder, Deposit-label rejection, duplicate-payment 409s, frozen-field/audit 422s, unlock recompute, event appends with idempotency keys); `docs/milestone-workflow.md` transition table; 27 new tests. `npm run checks` green (99/99), build compiles, prisma validate/generate ok.
- Previous: Session 04 client/project domain (v0.4.0): full client profile (contact/billing/timezone/country/notes/status) + project scheduling/terms/status; API create/edit/list/detail + `GET .../projects/:pid/summary` command center (total/paid/outstanding, current milestone, next action, statuses, recent activity; verified receipts only, no payments built); calm server-rendered `/app/*` pages from reusable components (no dashboard chrome). 10 new tests; `npm run checks` green (72/72), build compiles, prisma validate/generate ok.
- Previous: Session 03 identity/auth/ownership (v0.3.0): stateless HMAC sessions (signup/signin/signout/me + account settings + password rotation), workspace CRUD + owner-only member invites (owner/member/accountant_readonly), tenant clients/projects with server-side membership+role+IDOR gates, Store seam (InMemory for tests / Prisma for prod, no migration needed). 23 new tests (session unit + auth/ownership/IDOR/role integration); fixed magic-link tamper-test flake. Verified: `npm run checks` green (62/62 tests), `npm run build` compiles, `prisma validate` valid, `dist/server.js` live smoke. Files added: `src/lib/{session,authz,store}.ts`, `src/routes/{requestAuth,auth,workspaces,resources}.ts`, `tests/{unit/session,integration/auth}.test.ts`. Modified: `src/config/app.ts`, `src/routes/health.ts` (0.3.0), `package.json`, `tests/integration/http.test.ts`, `tests/unit/magicLink.test.ts`, `README.md`, `progress.md`. No secrets committed. No push.
  What failed:
- Nothing blocking. Full `npm run checks` green on final pass (286/286).
  Files changed:
- Added: `src/domain/notifications.ts`, `src/lib/notify.ts`, `src/lib/unsubscribe.ts`, `src/routes/notifications.ts`, `prisma/migrations/0011_notifications/migration.sql`, `tests/unit/notifications.test.ts`, `tests/integration/notifications.test.ts`
- Modified: `prisma/schema.prisma` (Notification kind/category/readAt + notification_preferences + notification_opt_outs), `src/lib/store.ts` (kind/category/readAt seam + prefs/opt-outs memory + Prisma + mappers + clear), `src/config/app.ts` (notification routes), `src/domain/timeline.ts` (NotificationQueued descriptor), `src/routes/resources.ts` (pause/unpause fan-out), `src/routes/portal.ts` (4 milestone-decision fan-outs), `src/routes/payments.ts` (webhook fan-out), `src/routes/paymentPlans.ts` (propose/accept/portal-accept/complete/default fan-outs), `src/routes/deliverables.ts` (release + portal-approve fan-outs), `src/routes/pages.ts` (notification center + project-page link), `src/routes/health.ts` (0.18.0), `package.json` (0.18.0), `tests/integration/http.test.ts` (version), `tests/integration/timeline.test.ts` (NotificationQueued tail), `tests/unit/timeline.test.ts` (descriptor coverage), `README.md` (notifications section), `progress.md` (this update)
- Added: `src/domain/protection.ts`, `src/routes/protection.ts`, `tests/unit/protection.test.ts`, `tests/integration/protection.test.ts`
- Modified: `src/config/app.ts` (protection routes), `src/routes/pages.ts` (Project-health page + project-page link), `src/routes/health.ts` (0.16.0), `package.json` (0.16.0), `tests/integration/http.test.ts` (version), `README.md` (project-health section + architecture map), `progress.md` (this update)
- Added: `src/domain/evidencePack.ts`, `src/routes/evidencePacks.ts`, `tests/unit/evidencePack.test.ts`, `tests/integration/evidencePacks.test.ts`
- Modified: `src/lib/store.ts` (evidence-pack seam memory + Prisma + mapper + clear), `src/config/app.ts` (evidence-pack routes), `src/routes/pages.ts` (evidence list + detail pages + project-page link), `src/routes/health.ts` (0.15.0), `package.json` (0.15.0), `tests/integration/http.test.ts` (version), `README.md` (evidence-pack section + architecture map), `progress.md` (this update)
- Added: `src/domain/timeline.ts`, `src/routes/timeline.ts`, `tests/unit/timeline.test.ts`, `tests/integration/timeline.test.ts`
- Modified: `src/lib/store.ts` (findProjectEventById memory + Prisma, listProjectEvents cap 100→500), `src/routes/resources.ts` (ProjectCreated on project create + event-sourced pause/unpause), `src/config/app.ts` (timeline + portal-timeline routes), `src/routes/pages.ts` (timeline list + detail pages + project-page link), `src/routes/health.ts` (0.14.0), `package.json` (0.14.0), `tests/integration/http.test.ts` (version), `tests/integration/clientProject.test.ts` (summary now asserts both Payment-requested and Project-created labels), `README.md` (evidence-timeline section), `progress.md` (this update)
- Added: `src/domain/paymentPlans.ts`, `src/routes/paymentPlans.ts`, `prisma/migrations/0010_payment_plans/migration.sql`, `tests/unit/paymentPlans.test.ts`, `tests/integration/paymentPlans.test.ts`
- Modified: `prisma/schema.prisma` (PaymentPlan currency/version/supersedesId/note/updatedAt + `PlanState.superseded`), `src/lib/store.ts` (payment-plan seam memory + Prisma + mappers + clear), `src/config/app.ts` (payment-plan routes), `src/domain/types.ts` (5 PaymentPlan events), `src/domain/events.ts` (plan reducer mapping), `src/routes/health.ts` (0.13.0), `package.json` (0.13.0), `tests/integration/http.test.ts` (version), `README.md` (payment-plans section), `progress.md` (this update)
- Previous session added: `src/domain/reminders.ts`, `src/routes/reminders.ts`, `prisma/migrations/0009_reminder_engine/migration.sql`, `tests/unit/reminders.test.ts`, `tests/integration/reminders.test.ts`
- Modified: `prisma/schema.prisma` (Notification audit columns + `projects.reminder_policy`), `src/lib/store.ts` (workspace/project policy fields + notification seam memory + Prisma + mappers + clear), `src/lib/providers.ts` (FakeEmailProvider + resolveEmailProvider), `src/routes/requestAuth.ts` (emailProvider dep), `src/config/app.ts` (email provider wiring + reminder routes), `src/domain/types.ts` (ReminderCancelled), `src/routes/health.ts` (0.12.0), `package.json` (0.12.0), `tests/integration/http.test.ts` (version), `README.md` (reminders section), `progress.md` (this update)
- Modified: `prisma/schema.prisma` (Approval decision/version/identity columns), `src/lib/store.ts` (Approval seam memory + Prisma + mappers + clear), `src/config/app.ts` (approval routes), `src/domain/types.ts` (ApprovalRejected/ApprovalDisputed), `src/domain/events.ts` (reducer), `src/domain/portal.ts` (allowlist), `src/routes/portal.ts` (milestone Approval rows + reject/dispute), `src/routes/deliverables.ts` (stale-reset + Approval rows + revision/reject/dispute/history), `src/routes/milestones.ts` (version-change approval reset), `src/routes/health.ts` (0.11.0), `package.json` (0.11.0), `tests/integration/http.test.ts` (version), `README.md` (approvals section), `progress.md` (this update)
- Modified: `src/lib/providers.ts` (FakeStorageProvider + resolveStorageProvider), `src/lib/store.ts` (deliverable/version seam memory + Prisma + mappers + clear), `src/config/app.ts` (storage provider wiring + deliverable routes), `src/routes/requestAuth.ts` (storageProvider dep), `prisma/schema.prisma` (deliverable lifecycle/staging/payload columns), `src/routes/health.ts` (0.10.0), `package.json` (0.10.0), `tests/integration/http.test.ts` (version), `README.md` (deliverables section), `progress.md` (this update)
- Previous session modified: `src/routes/payments.ts` (claim + history + reconciliation + diagnostics + reconciliation/run + persistent webhook markers + out-of-order notes), `src/routes/portal.ts` (client claim endpoint), `src/domain/reconciliation.ts` (out-of-order audit persistence), `src/routes/health.ts` (0.9.0), `package.json` (0.9.0), `tests/integration/http.test.ts` (version), `README.md` (payment-trust section)
- Previous session added: `src/domain/payments.ts`, `src/routes/payments.ts`, `prisma/migrations/0006_payment_lifecycle/migration.sql`, `tests/unit/payments.test.ts`, `tests/integration/payments.test.ts`
- Modified: `src/lib/providers.ts` (checkout/refund/retrieve + Stripe/Fake/Noop + resolver), `src/lib/store.ts` (payment lifecycle seam memory + Prisma + mapper, id-aware create, lifecycle-only update, seed backfill), `src/config/app.ts` (raw-body parser + provider/secret wiring + payments routes), `src/routes/requestAuth.ts` (provider/secret deps), `src/routes/portal.ts` (hosted checkout on pay + read-only status/success pages), `src/domain/money.ts` + `src/domain/portal.ts` + `src/domain/projectView.ts` (verified `paid`), `prisma/schema.prisma` (PaymentState), `src/routes/health.ts` (0.8.0), `package.json` (0.8.0), `tests/integration/http.test.ts` (version), `README.md` (payments section), `progress.md` (this update)
- Untracked-but-ignored: `node_modules/`, `dist/` (build artifact; gitignored)

## NEXT SESSION

Objective:

- Session 19: bring up live Postgres (`docker compose up -d db`), apply migrations (`0001_foundation` + `0002_client_project_domain` + `0003_milestone_engine` + `0004_agreement_terms` + `0005_portal_access` + `0006_payment_lifecycle` + `0007_deliverables` + `0008_formal_approvals` + `0009_reminder_engine` + `0010_payment_plans` + `0011_notifications`; Session 14 needed no migration — timeline reads the append-only events table) with `npx prisma migrate dev`, prove content-guard triggers + idempotency UNIQUEs (incl. notifications + preferences/opt-out uniques) + `projects_dates_chk` + `milestones_project_order_uniq` + `guard_agreement_immutability()` (content UPDATE rejected, lifecycle UPDATE allowed, DELETE rejected) + `guard_payment_immutability()` (money/provider UPDATE rejected, state UPDATE allowed, DELETE rejected) + deliverable status/staging CHECKs + approvals version-pin/decision CHECKs + notifications idempotency UNIQUE + events `idempotency_key` UNIQUE (webhook-marker + reminder + notification replay across restarts) + portal-link expiry/revocation against the real DB, smoke `/health` expecting `db:"up"`, AND run a live-DB pass of the identity + client/project/milestone/agreement/portal/payments/trust/deliverables/approvals/reminders/payment-plans/notifications slices against `PrismaStore` (signup → workspace → invite → client → project → milestones → transitions → reorder → agreement draft → send → accept → v2 supersede → portal-link issue → client overview → approve (Approval row + event) → revision/reject/dispute probes → claim (totals unchanged) → checkout → verified webhook paid (→ payment_received notice) → replay-duplicate → out-of-order probe → partial coverage → refund/reconcile → reconciliation report `balanced` + diagnostics + history → deliverable create → version → submit → share-preview → client review → approve (v1) → upload v2 (status resets to submitted, v1 approval stays true, release still locked) → approve v2 → payment_pending → paid (verified) → release (→ deliverable_released notice) → preview-allowed + final-allowed + staging publish → transfer-request → transfer-complete → approval history `effective.isApproved` → reminder policy PUT → schedule → run-due tick → manual escalation with jurisdiction → cancel probe → notification prefs PUT → overdue tick → dispatch-due tick → opt-out + unsubscribe-token probe, plus cross-tenant 403/404 probes). Then build the next vertical slice: release automation on verified-paid. Portal success pages stay read-only.
  Important context:
- Read this progress.md + all docs/ files first (now five: product-requirements, domain-model, user-flows, security-principles, milestone-workflow, agreement-terms). Preserve invariants (§Invariants) and event vocabulary. Trust ladder + 3-dimension state model are normative. Stack is decided (see Completed §2026-09-18) — do not re-debate without concrete reason recorded here. Auth model is decided (stateless HMAC sessions + Store seam + owner/member/accountant_readonly) — extend it, don't replace it. Agreement model is decided (immutable versions + hash fingerprints + lifecycle-only store seam + DB content guard) — extend it, don't replace it. Portal model is decided (single-project magic links, sha256-stored, revocation-checked; client-safe projection via `domain/portal.ts`; claim-never-paid) — extend it, don't replace it. Payment model is decided (Stripe-behind-seam, hosted checkout only, no escrow, no card storage; authoritative state from verified webhooks/retrieve only; lifecycle-only store seam + DB linkage guard; success pages read-only) — extend it, don't replace it. Deliverable model is decided (8-state controlled-delivery lifecycle; REVIEW vs OWN separation with honest no-DRM notice; versions carry mixed files/links/previews/descriptions; safe handling via UUID keys + allowlists + caps; signed URLs gated by release — previews ≤1h once reviewable, finals ≤15min only when released, 423 LOCKED otherwise; release needs version-pinned approval + verified paid except flagged override; staging URL vs final transfer are distinct states) — extend it, don't replace it. Approval model is decided (Session 11: append-only version-pinned decisions approved/revision_requested/rejected/disputed in `domain/approvals.ts` + Approval rows with exactly-one-version + decision CHECKs + hashed device metadata only; new versions reset status without rewriting history; only the latest decision pinning the current version authorizes release) — extend it, don't replace it. Reminder model is decided (Session 12: configurable policies in `domain/reminders.ts` — project override → workspace defaults → built-in default, never a universal schedule; notifications are the audit rows with idempotency UNIQUEs; escalation/work-paused steps are manual-only + jurisdiction-aware, never auto-sent; scheduling stops on verified paid) — extend it, don't replace it. Payment-plan model is decided (Session 13: milestone-scoped schedules in `domain/paymentPlans.ts` — installments sum exactly to the outstanding obligation; originalAmountCents write-once; modified schedules are new versions via supersedesId, never edits; installments settle only against verified receipts; milestones move plan_active on acceptance and paid on full settlement; reminders are system-voiced via the email seam with Notification audit rows) — extend it, don't replace it.
- Amounts integer cents; timestamps UTC; UUIDs in URLs; neutral reminder copy; disclaimers required in portal/export later (agreement disclaimer already ships on every agreement + portal response).
  Do not repeat:
- Do not re-debate escrow (decided: no MVP escrow, use provider). Do not collapse work/payment/delivery into single status. Do not add PATCH/PUT to agreements (decided: corrections are new versions). Do not promise DRM that does not exist (decided: honest limits notice on every preview/lock response). Do not serve final assets before release (decided: signed URLs gated — previews once reviewable, finals only when released, 423 LOCKED otherwise). Do not start full UI before data+event foundation + security tests. Do not write secrets into repo.

## Future

- V1.1: retainers, Net-terms automation, trust-tier automation, accountant read-only seat.
- V2: multi-seat workspaces, hash-chained evidence (`prev_hash`) + third-party timestamping, PPV/leakage analytics (honest methodology), additional payment providers, SMS channels.
- Ops: SPF/DKIM/DMARC, secret rotation runbook, retention/GDPR-anonymization rule with counsel sign-off, accessibility (WCAG 2.2 AA) audit, perf budgets (dashboard p95 <2s, portal <1.5s) validation.
